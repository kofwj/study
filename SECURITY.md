# 安全检查清单

## 部署前必须检查

### 🔴 关键安全配置

#### 1. PostgreSQL 数据库密码
**位置**: `.env` 文件

```bash
# 必须设置强密码，不要使用默认值
POSTGRES_PASSWORD=<至少16位随机字符串>
DATABASE_APP_PASSWORD=<至少16位随机字符串>
```

**验证**: 密码长度至少16位，包含大小写字母、数字和特殊字符

#### 2. Session Secret Key
**位置**: `.env` 文件

```bash
# 必须设置，避免容器重启丢失
SECRET_KEY=<64位十六进制字符串>
```

**生成方法**:
```bash
python3 -c "import os; print(os.urandom(32).hex())"
```

#### 3. Docker 端口绑定
已修复为只监听 localhost: `192.168.100.5:9000:8000`

### 已修复的高危Bug (2024-09-08)

1. 硬编码数据库密码 → 改用环境变量
2. Secret Key丢失 → 持久化到/data目录
3. 并发余额超支 → 添加事务锁
4. 连接泄漏 → 使用try-finally
5. 弱密码 → 孩子密码6位+禁止重复/连续
6. 输入验证 → 阳光上限10000
7. Docker端口暴露 → 只监听127.0.0.1

详见完整文档。
