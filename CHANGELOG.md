# 更新记录

## 2026-09-18 v0.3.67 - 家长工作台：开关换成真开关 + 按钮高度统一

- **开关**：家长端「开 / 关」以前是带文字的胶囊按钮（`.admin .toggle`），每处还要自己写
  `{{ x ? '开' : '关' }}`，状态文字在行里常常再重复一遍。现在统一成新的
  `frontend/src/components/AdminSwitch.vue` —— 开 = **品牌蓝 `var(--brand)` 实心胶囊 + 白色圆钮**（滑到右边），
  关 = **iOS 那种浅灰轨道（`#e9e9ea`）+ 白钮在左**（浅灰在白卡片上本来就淡，所以靠一圈 16% 的内描边
  定住形状、钮带阴影，和 iOS 一样）；48×28（`size="sm"` 备用 40×24），圆钮 `translateX(--sw-w - --sw-h)` 用 `--spring` 滑过去；
  按下时整块不缩放，动的是里面那颗钮。无障碍：原生 `<button>` + `role="switch"` + `aria-checked`，
  Tab 可聚焦、Enter/Space 可切；`label` 只给读屏（开关左侧本来就有说明文字，不再重复渲染）。
- 替换 **14 处**：家庭 3（邀请码保护 / 打卡时间窗 / 储蓄所营业）、英语单词 4（单词练习 / 词书锁 / 朗读 / 自动读）、
  阳光 3（银行 / 利息 / 扣分）、已学到 4（显示学科 / 进度锁 / 图鉴 / 基地）。
  事件接法不变：值归壳的那几处继续 `$emit`，本页即时保存的仍直接调；阳光页利息开关从模板里那行
  `enabled = !enabled; saveBankInterest()` 收进 `toggleInterest()`。
- **按钮统一**：在这之前「保存 / 确认 / 添加 / 删除 / 批准」各写各的 padding，高 30–34px、
  圆角 md 和 lg 混着来，同一行里也一高一矮。现在收成一套（`adminBase.css`）：
  动作按钮 `.ok` / `.del` / `.sec` **高 34px、圆角 md、14px 字**；整页主 CTA 单独一档 `.ok.lg`（44px，
  只用在「创建并进入 / 我已抄好」这种单独一张卡的按钮）；表格里那种极小「×」用 `.del.xs`（28px，
  任务页破纪录指标两处）；次要文字按钮（取消 / 改 / 放弃 / 撤回）在 `.ops` / `.apv-right` / `.w-next` /
  阈值行（`.band-actions`）里对齐 34px；筛选件 `.chip`（扣分筹码）与 `.subj-tab`（科目 tab）统一 32px。
  另外三处收口：复习页三个动作按钮（会了 / 还不熟 / 已掌握）删掉 12px 覆盖、跟统一字阶走；
  `.sec:disabled` 的 0.6 与 `.ok/.del` 的 0.55 合成一套；总览卡片里那个 `<span class="ok fam-go">去复习</span>`
  单独压成 26px 小片（卡片本身是 `<button>`，span 不能再嵌 button，但也不该长得像主按钮）。
  顺手删掉重复定义的 `.admin .sec` padding/圆角和重复的 `.admin .ok.wide`。
- 体检：`bash scripts/admin-refactor/run_checks.sh --with-ssr` **退出码 0**（23 个子组件样式行 0 个「拿不到样式」；
  20 个 SSR 用例 0 警告 / 0 缺失 / 0 个 undefined），`bash scripts/smoke_frontend.sh` 通过（1893 modules，1.02s）。
- ⚠️ 顺带修了体检自己的一个**哑火点**：`ssr-cursor.mjs` 原来数的是 `class="toggle on"` / `class="toggle"`，
  开关换组件之后那行会永远 `on 0 / off 0`「看着通过、其实什么都没验」。现在改成数 `role="switch"` 的个数
  加 `aria-checked` 的真假（期望 6 个 / on 4 / off 2），对不上就打「缺失」，`run_ssr.sh` 会红。

## 2026-09-16 v0.3.66 - 英语默写：撇号要孩子自己敲

- 家长反馈「英语单词的撇号还是要自己输入」：`let's` / `It's` / `o'clock` 这种**撇号是单词的一部分**，
  以前它跟句号一样是「固定格」—— 看得见、但自动补上，孩子**永远不会练到它**，真到纸笔默写就会漏。
- 新口径：**字母和撇号都要孩子敲；空格、句末标点仍然免敲**（句号问号敲它只是让孩子多按一次符号页）。
  页面上撇号变成**看得见、必须填**的格子：孩子敲 `lets` 时 `s` 会掉进撇号那一格、最后一格空着 ——
  一眼就能看出漏了撇号。
- 改动：`wordSpell.js` 的 `lettersOf` → **`typedOf`**（留字母 + 撇号，弯撇号 ’ 折成直的，
  **保留大小写**给页面显示）；`assembleSpelling` 只填「孩子敲了的格子」，所以漏敲撇号会拼成
  `lets` ≠ `let's` → 判错；`slotCells` 把撇号当要敲的格；后端 `words._letters_only` → **`_typed_only`**
  （判分时才忽略大小写，同样只留字母和撇号）。前后端同口径，两边都有测试钉住。
- 实测（`let's`）：敲 `lets` → 提交 `lets` → **判错**；敲 `let's` 或 `LET’S`（弯撇号）→ **判对**；
  `Good morning.` 漏敲句号仍然**判对**（句末标点免敲）。
- ⚠️ 这是**故意变严**：词库里 14 个带撇号的词条（`It's your turn.`、`What's the matter?`、`o'clock`、
  `on one's way back`…）从此漏敲撇号会判错。历史记录不回算。
- 测试：`check_word_spell.mjs` 10727 → **12247 条**（改成两套口径分开验：敲「字母 + 撇号」必须拼出原答案；
  **漏敲撇号必须拼不出原答案**；撇号格必须是「要敲的格」；`typedOf` 只留字母 + 撇号）；
  后端 `test_words.py` 两个旧口径用例反过来写（`It's` 带撇号 True / `its` 漏撇号 False / `o'clock` /
  `don't` + accept），后端 **138 全绿**。

## 2026-09-16 v0.3.65 - 大队委填空自动判分 + 家长端成绩 + 英语默写不再倒着出字

- **① 大队委填空改成后端自动判分**（以前是孩子自己点「答对了 / 没答对」）：
  练习模式填空现在有真实输入框（几个空几个框），每个空一个答案、用 `\u001f` 连接提交，
  **后端按空位规范化比较判分，客户端传的 `correct` 一律不信**
  （`_norm_text`：全角→半角、去空白、去中英文标点、忽略大小写；空位数对不上直接算错）。
  每题可带一份按空位给的 `accept` 同义答案清单（新列 `quiz_questions.accept_json`）。
  页面双击打开（`file://`）时连不上后端，所以 `QUIZ-JUDGE` 段在 JS 里有一份同口径实现。
- **② 模拟考也要真写填空**：原来模拟考对填空/问答写的是「在纸上写，交卷后一起对」，
  却仍会 `apiAttempt('', null)` —— 填空改成后端判分之后，空答案会被记成**错**。
  现在模拟考填空同样出输入框、交卷按后端判；问答仍自评、但答案会入库。
- **③ 问答仍自评，但必须先把答案写下来**：参考答案是整句，机器判不可靠；
  空着点「答对了」会被后端挡回 400。答案入库，家长能看到孩子原话 vs 参考答案。
- **④ 家长端终于能看大队委成绩**：`GET /api/admin/quiz/summary` + 侧栏「学习 · 大队委」。
  **刻意把可信度分开报**：`机器判分`（选择/多选/填空）、`孩子自评`（问答）、以及改版前的老记录
  （新列 `quiz_attempts.judged_by`，老行留空 → 单独标「改版前，分不清」）。
  逐题明细带孩子写的原话和参考答案，默认只看错题。
- **⑤ 判分口径做成前后端共享用例**：`data/quiz_judge_cases.json` 一份用例，
  `node scripts/check_quiz_judge.mjs` 和 `backend/test_quiz_phase2.py` 都读它。
- **⑥ 英语默写输入重做**：中文平板上 `inputmode="text"` 会弹拼音；
  更严重的是 **Android WebView 把插入点钉在 0 位**，按顺序敲 `good` 会显示成 `doog`。
  现在字母和光标都由页面自己维护（`state.letters` / `state.caret`），输入框只当按键来源，
  读完立刻清空。退格走 `keydown` 拦下来。顺带修了「再写一次 / 看一眼」不清空上一次错字。
  SPA 弹窗的 `inputmode` 一并改成 `latin`。
- 测试：`test_quiz_phase2.py` 12 → **17 条**；`check_word_spell.mjs` 10715 → **10727 条**；
  大队委填空判分 JS 侧 **28 条断言**。后端 **131 → 138 全绿**。

## 2026-09-16 v0.3.64 - 打卡时间窗 + 储蓄所营业时间，家长可配

- **① 储蓄所营业时间家长可配**：原来 `BANK_OPEN_HOUR = 8` / `BANK_CLOSE_HOUR = 20` **写死在代码里**
  （连提示文案都是硬编码的，前端还另抄了一份兜底文案）。现在和打卡时间窗同一个做法：
  家族列 `bank_hours_enabled / bank_open_hour / bank_close_hour` + `family_bank_hours()` +
  `PUT /api/admin/family/bank-hours`（`enabled` 开关、0–23 / 1–24 clamp、「开门必须早于打烊」校验），
  `bank_window()` 的提示改成按配置拼（`储蓄所 每天 06:00 到 22:00 营业，现在已经打烊`）。
  **默认仍是 8:00–20:00，行为不变**。
- **② 打卡时间窗也做成家族设置，并接上家长端**：以前只有全局常量 `7:00–21:00`，家长撞到「现在打烊」改不了。
  现在家族列 `checkin_hours_enabled / checkin_open_hour / checkin_close_hour` +
  `PUT /api/admin/family/checkin-hours`；「家庭」页多一段 **打卡与储蓄所时间**：两项各一个开关 +
  开门/打烊整点，保存后进孩子端立即生效。关掉某一项 = 那件事全天都能做。
- 测试：`test_bank_hours.py` 3 → **7 条**（能读能写 + 两端 clamp + 开门晚于打烊报 400 + 关掉全天可存取、
  `bank_window` 按配置判定（6:00–22:00 那家 21:00 还开着、默认那家已打烊）、跨家隔离、打卡时间窗端点可达）；
  后端 **127 → 131 全绿**。

## 2026-09-16 v0.3.63 - 英语复习改成「小关 + 发音 + 收获」，阳光不再看正确率

家长反馈「这和预期的单词复习差距较大」：原来的 `/word/` 是「抽 20 个词串行考一遍 → 给一个正确率百分比」，
本质是**测验器**而不是**练习场**。这一版按「10–11 岁孩子愿意每天来一次」的方向重做体验层。

- **① 补上发音（这一页以前一句话都不念）**：家长端早就配好了 `tts` / `tts_autoplay` / `tts_lang`，
  `/api/words/game/start` 也一直在返回这三个值，`components/WordPractice.vue` 里有现成实现 ——
  但 `/word/` 从上线起就没用过。现在：**认一认自动念**（题面本来就是英文，不泄题）、
  **连一连点左边英文就念**、**对错都把词念一遍**、默写时多一个 **🔊「听一听」**当提示
  （比「忘了，看一眼」轻一档）。优先走平板壳的原生桥 `SunshineTts`，退回浏览器 `speechSynthesis`。
  **后端零改动**。
- **② 阳光口径从「正确率」改成「完成 + 进步」**：原来 60 分以下**一分不发**（尖子生和错多的孩子
  差距被放大成 0 与 10）；现在**写过一个词 0.25 分、其中写对的再 0.25 分，错题不扣分** ——
  20 词全对 = 10，20 词做完但全错 = 5（来了就有保底），一天上限仍是 10。
  服务端口径换成 `sunshine_for_progress(done, right)`（`words_today()` 一次查出「写了几个 / 对几个」）；
  正确率**仍然算、仍然给家长看**（`best_score` 不变），只是不再拿它决定孩子的钱。
- **③ 一局拆成小关**：整局步骤按「一关几步」切开（默认 6，**家长可配**，见 ⑦），
  **做完一关就弹一次星星**（3 星 = 这一关写的全对），不用等 20 题全做完才看到「忙完了」；
  顶部进度改成**倒着数**：`第 N 关 · 还剩 M 个词`（原来正着数「第 3/26 题 · 连击 0」）。
  小关规则是纯函数 `chunkLevels()` / `levelEnds()`（有断言）。
- **④ 结算页从「宣判」改成「收获」**：去掉 44px 的大号百分比，改成三行
  `今天练了 N 个词 / 已经会写 N 个 / 还要再来一回 N 个`；配一句 `阳光 +X · 今天英语阳光 X/10`。
  **不再给孩子看到一个会随难度上升而必然下降的分数**。
- **⑤ 顺手清掉两处「考试味」**：认一认的提示不再写「不计分」、页脚改成「错的词明天还会再来，不用怕写错」。
- **⑥ 五上 199 条例句草稿**：`data/words_examples_g5s1.draft.json`（机器可读）+ `words_examples_g5s1.review.md`
  （按单元分节的复核表格）。**199/199 条全覆盖，无重复、无空值、无超 12 词**；其中 **22 条机器标了「要复核」**
  （用法/搭配/变形，例如 `do exercise` → `does exercise`、`put ... in order` 的省略号填法、`skip` 的英式含义）。
  分 4 片生成后合并，中间分片文件已删除。**待人工复核后再入库** —— 词表里 `example_en`/`example_cn` 两个字段
  一直存在、一直空着（0% 填充），这一版只出草稿，**不动后端、不动词库**。
- **⑦ 小关步数做成家长端旋钮**：页面早就在读 `config.level_size`，但后端没这个键（写死 6）。
  现在「英语单词」页「怎么练」那排多一个 **一关几步（3–10，默认 6）**：
  `kid_config` 新增 `level_size`（`CFG_LEVEL_SIZE`，`_clamp_int(…, 6, 3, 10)`），`WordConfigIn` 加 `level_size`，
  随 `/api/words/game/start` 的 `config` 下发给页面。实测同一局（约 30 步）：
  一关 3 步 → 中途弹 **9** 次星星；6 步 → **4** 次；10 步 → **2** 次。
- **⑧ 修一个既有漏账（英语在本周阳光图里一直是 0）**：家长端「本周阳光」的图与 `by_reason` 分类靠
  `EARN_REASONS` / `PATH_OF` 两个白名单，而 `/word/` 结算用的理由 `word_game` **不在里面**
  （`word_daily` / `word_perfect` 从 v0.3.53 起就不发了）—— 结果孩子每天从 `/word/` 拿到的阳光
  **不进本周图、也不进「英语」分类**。现在把 `word_game` 加进两个表（历史理由保留，老流水照旧能画）。
  累计阳光 / 等级 / 余额走的是排除法，本来就没漏（已核对 `earned()`）。
- 口径变化同步到两侧：`frontend/src/wordGame.js` 的 `sunshineForProgress` ↔ `backend/words.py` 的
  `sunshine_for_progress`（两边都有测试钉住，防止漂移）。
- 测试：`check_word_game.mjs` 102 → **119 条**（阳光档位 15 条换成 18 条「完成 + 进步」档位，新增 14 条小关切分）；
  后端 `test_word_game.py` 6 条改成新口径（含「错题不扣分」「累计写对才补钱」，另扩「一关几步」的默认值与两端 clamp），
  `test_words.py` 家长端数字跟着改（70 分那条从阳光 2 变成阳光 8），
  `test_ledger_summary.py` 新增一条 `word_game` 流水（⑧ 的回归）。后端 **127 全绿**；
  新增 `scripts/check_word_page.mjs`：最小 DOM 桩把 `/word/` 整局跑通（认 → 写 → 写错重写 → 小关 → 结算），
  并支持用 `LEVEL_SIZE` 环境变量换档，把「一关步数越小、星星弹得越勤」变成可复现断言；
  **手动跑** —— 它依赖定时器，故意不进 `pre_deploy`，免得时序抖动影响部署门禁。
  SSR 桩 `ssr-words.mjs` 补 `level_size`；`pre_deploy.sh` + SSR 体检全绿。

## 2026-09-16 v0.3.62 - 每局换顺序 + 还没开练前每次打开换一批词

- **① 每次打开都换顺序**：`/word/` 每次进页面（和点「再练一遍」）都把这一局的词**打乱**——
  第一题、整局顺序都不再固定，孩子背不了「第几题是什么」；词本身不变
- **② 还没作答时每次打开换一批词**：今天这局**一题都没做**时，重新从「到期池 + 未学词」取一批
  （未学词随机采样）；**答过一题就锁定**同一批 —— 保证「这一局词数 = 这一轮的分母」和结算口径不被换词搅乱
- 换词**不换会话**（还是今天那一条），也**不动钱**：分母、当天最好一轮、一天最多 10 都不变；
  SPA 那条流程（孩子端「今天」页的单词弹窗）行为不变，只有 `/word/` 开局会换
- ⚠️ 注意：换一批只在**候选词比一局词数多**时才看得出区别。比如五上第一单元整本 20 个词、
  而家长设的「一局词数」也是 20，那整本都会进这一局，换一批就只有**顺序**变。
  想让每批的词真的不同，把「一局词数」设小一点（例如 10），每批就是不同的那一半
- 新增断言：`shuffleQueue` 6 条（顺序变、词不丢不重、同随机源可复现、不改原数组、空值不炸）→ 共 **102 条**；
  后端 1 条（未作答换一批 / 会话不变 / 作答后锁定）；后端 **127 全绿**

## 2026-09-16 v0.3.61 - 家长端看得见「这批词从哪来」

- 「英语单词」页「今天」那排下面多一行解释：`这批词来自：三年级上 Unit 1（12 个）、五年级上 Unit 1（8 个） · 新词：还没选词书 → 不会出新词`
- 四种情况分别说清：还没开局 / 新词词书没选（→ 不会出新词）/ 被词书锁挡住（→ 去「已学到」设英语进度）/ 自定义范围（→ 从哪本往后取）
- 来源：`words.today_books()`（今天这一局按书分组统计）+ `words.new_book_note()`（新词那本 + 是否被挡）+ 纯函数
  `words.source_sentence()`；只读现有数据，**不动挑词逻辑、不加接口**；读不到就返回空串，这一行不显示
- 新增 1 条后端用例（`source_sentence` 六种组合）+ stats 断言；SSR 桩 `ssr-words.mjs` 加该字段；后端 **126 全绿**

## 2026-09-16 v0.3.60 - 孩子端英语复习入口卡 + 打卡弹窗层级（P4-b）

- 孩子端「今天」页最前面多一张**英语复习卡**（时间胶囊之后、每日打卡之前）：
  `今天 7/10 词 · 到期 12 个` + 目标进度条 +「还差 3 个」，点整张卡或「开一局」直接去 `/word/`；
  英语关掉时这张卡不出现
- 卡片文案由 `wordGame.js` 的 `entryCardText()` 算（纯函数，`check_word_game.mjs` 新增 16 条断言钉住
  五种状态：有到期没练 / 练了一半 / 今天达标 / 今天没词 / 英语关掉），组件只渲染
- **每日打卡弹窗统一层级**（`DailyDialog.vue`）：有去处的任务（大队委题库这类）主行动改成通栏的「去做」，
  「打卡，赚阳光」降为次按钮；没去处的任务仍是「打卡」当主按钮；「怎么做」改成浅色说明块；
  题库没做完时只留「先去做题库」+ 一句提示（打卡按钮照旧不出现）
- `/api/words/today` 现在带 `goal`（今天写了几个 / 目标 / 是否达标）：孩子端卡片与 SPA 共用；
  `today_payload` 外面套一层统一补，目标读不到也不影响今天这一组（不给主流程添新故障点）
- 新增两个孩子端 SSR 桩：`ssr-wordgamecard.mjs`（入口卡五种状态）、`ssr-daily.mjs`（弹窗空态，
  打开态 SSR 驱动不了 —— 用临时样张页在真浏览器里看过三种状态）
- 后端 **125 全绿**；`check_word_game.mjs` 80 → **96 条**；拼写 10715 条 0 失败；build 冒烟 + 静态/SSR 全过

## 2026-09-16 v0.3.59 - 家长端看得见英语复习（P4-a）

- 「英语单词」页「今天」那排新增三个盒子：**今天写了 N/目标**、**最好一轮 X 分**、**今天阳光 N/10**；
  下面两行是后端拼好的人话：「今天写了 20 词（目标 10，已达标） · 最好一轮 70 分 · 阳光 2/10」
- 总览页「本周盯点」下面新增「**本周英语**」：每个孩子一句「这周练了 4 天，首轮正确率 78%；今天写了 20/10 词（达标）」
- 口径与 `/word/` 完全一致（服务端算：今天写过的去重词数、当天最好一轮得分、阳光、是否达标；本周用既有 week_stats）
- 只加字段：`/api/admin/words/stats` 加 `today` / `week` / `today_sentence` / `week_sentence`，
  `/api/admin/weekly` 的 `kids[]` 加 `words.sentence`；**不加接口、不迁移、不动 ledger**
- 读不到时退化成「暂时读不到英语记录」，绝不把 stats / 周报拖垮（B4 那次空白页的教训）
- 新增 SSR fixture `ssr-words.mjs`（这个页面以前没有 fixture），`ssr-insights.mjs` 加「本周英语」两例（含老后端没这字段）；
  后端新增 3 条用例（文案 6 种组合 / 数字与钱对得上 / weekly 那句），**125 全绿**

## 2026-09-16 v0.3.58 - 修拼写判分（撇号被当成要敲的字母）

- 症状：带撇号的词条（家长端的「错 N 次」）**怎么改都判错** —— 孩子按槽位敲、不敲撇号，整串就错位一格：
  `It's your turn.` 被拼成 `Itsy ourt urn.`。词库里有 14 条这种词条，五年级那本书（g5s1）就有 3 条
- 根因：v0.3.52 把句号/问号/逗号当成了「答案固定」，但撇号仍留在「要孩子敲的字母」里（`/[A-Za-z']/`），
  而槽位上看不见撇号 —— 孩子根本没有办法知道那里要敲一个字符
- 前端 `frontend/src/wordSpell.js`：撇号跟句号一样是**固定格**（看得见、不用敲）；`lettersOf` 只留 A–Z/a–z
- 后端 `words._spell_ok`：改成**只比字母**（空格、标点、撇号、大小写都不参与判分）——
  这样即使孩子手机上还是旧版页面，只要后端更新了，判分也已经对了
- `scripts/check_word_spell.mjs`：753 词条 × 5 种敲法（只敲字母 / 连标点一起敲 / 末尾多敲句号 / 字母之间乱加空格 / 全大写）= **10715 条断言**失败 0
- `backend/test_words.py` 新增 2 条：判分规则（少一个/多一个字母仍判错、`accept` 同理）+ 把 `It's your turn.` 造成到期词的真实复现；后端 **122 全绿**

## 2026-09-16 v0.3.57 - 静态资源 gzip + 长期缓存

- 静态 JS/CSS/图片走 gzip（约 232KB 的主包压到更小），`/assets/`、`/sprites/`、`/sounds/` 缓存一年（文件名带 hash，更新会换名）
- HTML 和 `sw.js` 仍是 `no-cache`，发新版孩子端还能拿到

## 2026-09-16 v0.3.56 - 英语复习加题型与每日目标（P3）

- `/word/` 三种题型：**认**（给英文选中文，4 选 1）/ **写**（拼写，唯一计分）/ **连一连**（熟词配对板）
- 题型按掌握度自动切：新词与老错先「认」、熟词（`interval_idx ≥ 2` 且连对 ≥ 2）先「连一连」、其余直接写；每个词最后都要默写 —— 分数与分母口径完全没动
- 「连一连」= **点选不拖拽**（先点左再点右，再点一下取消）、**家长端可设每块 3–6 词（默认 5）**、**一局最多 0–3 块（默认 3，0 = 不玩）**；配错红一下弹回来，不扣分、不重排、不算连击
- 认 / 连一连**不计分、不发钱、也零请求**（`study` 既不改 SRS 也不影响钱，SRS 只由「写」推进；顺带省掉「再练一遍」轮的 409）
- 新增「**每日目标环**」：今天写够 N 个词点亮（默认 10，5–40，**家长端可设 0 = 关闭**），局中写对就 +1、结算按服务端数字对齐，达标只变绿 + 🎉 + 一句话，**不发钱**
- 服务端新增 `scored_words`（今天 `phase='spell'` 的去重词数）/ `goal` / `goal_done`，`start`、`GET game`、`settle` 都带；每题多带 `progress`（页面据此切题型）
- 家长端「英语」页新增三个旋钮：**每日目标**（0–40）、**连线每块**（3–6）、**连线块数**（0–3）；一局**开局时**读一次，改完下一局生效
- `frontend/src/wordGame.js` 新增 `questionPlan` / `buildOptions` / `matchGroups` / `buildMatchBoard` / `planSession` 五个纯函数；`scripts/check_word_game.mjs` 断言 24 → **80 条**
- 测试：`backend/test_word_game.py` 新增 5 条（clamp、`scored_words` 去重、达标不发钱、块数 0、`progress` 回灌），全仓 **118 全绿**；静态 + SSR 体检、build 冒烟全过

## 2026-09-16 v0.3.55 - 英语复习开始发阳光（P2）

- `/word/` 一轮答完就结算：当天按**最好一轮**的成绩给额度（60 分以下 0；60–100 线性到 10，向下取整：70→2、80→5、95→8、100→10），**只补差额**（先 70 分拿 2，后来 80 分补 3）；一天最多 10
- 一轮得分由**服务端算**：`word_attempts` 里 `phase='spell'` 按 `attempt_no` 分组，分母是这一局词数（跳过/没答当没对：14 对 + 6 没交 → 70 分，不是 100）；不信客户端上报
- 做完一轮就等于**今天打卡**（写 `checkins`，幂等）；连击与全勤照旧；不再有单独的打卡阳光
- 不发星尘、不碰宝箱（宝箱仍按连击天数）
- 家长端「英语单词」页新增「一局词数」（默认 20，10–40）；`/word/` 开局按它取词，到期优先、不足用未学词补满
- 拼写接口放宽一处（有意）：同一词在 `attempt_no>1`（「再练一遍」轮）允许再判；同一轮内仍只判一次、走缓存
- 迁移 044：`word_game` 纳入一次性流水幂等（同一天同一档位只发一次）
- 测试：新增 `backend/test_word_game.py`（6 条，含 70→2 / 80→补 3 / 75→不发 / 跳过按词数分母 / 重复结算不多发 / 55 分不发钱但打卡 / 跨家庭隔离 / 幂等索引）

## 2026-09-16 v0.3.54 - 英语复习独立页 /word/（P1 骨架）

- 新增独立页 `/word/`（照大队委 `/quiz/` 的做法）：读今天的单词会话 → 逐词默写 → 结算（正确率 / 答对 / 最高连击）+「再练一遍 / 回今天」
- 拼写判分复用 `frontend/src/wordSpell.js`（标点与空格自动带出，孩子只敲字母）——页面作为 Vite 多入口构建，不再另抄一份判分逻辑
- 规则（算分 / 连击 / 阳光档位）抽到 `frontend/src/wordGame.js`，配 `scripts/check_word_game.mjs` 24 条断言（60→0、70→2、80→5、95→8、100→10）
- **这一版不发阳光、不写签到**（下一版接）；页面加载/提交失败都显示可读提示 + 重试，不会白屏

## 2026-09-16 v0.3.53 - 单词流程不再发阳光

- 去掉 `word_daily`（默认 3）与 `word_perfect`（默认 2）两笔：单词背默不再直接发阳光，家长端「英语单词」页的两项设置也撤掉
- 英语阳光改由独立页 `/word/` 按**当天最好成绩**结算（≤10/天，60 分以下 0、60–100 线性）——那部分在下一版上线；**这段时间单词这块的阳光为 0**，其它安全：打卡、每日任务、考试、宝箱等照旧
- 历史流水一律不动（ledger 是唯一真相）；`word_sessions` 的两个额度列保留但写 0，旧的完成会话仍如实显示当年发了多少
- 孩子端两处「+N 阳光」提示随之去掉（改成「· 新词」）；后端 `_first_try_all_right` 与两个配置常量成为死代码，已删

## 2026-09-16 v0.3.52 - 修拼写题：答案里的句号/问号/叹号被当成字母槽位

- 现象：带标点的句型（`Good morning.` / `Nice to meet you!` …）怎么敲都判错，家长端「英语单词」页一直挂「错 N 次」
- 根因：槽位显示与提交文本这两份各抄一遍的逻辑，只把空格和连字符当「固定字符」，其余标点被当成一个字母槽位；而输入框只收字母和撇号 → 标点丢掉或吃掉一个字母
- 修：抽成一个纯函数模块 `frontend/src/wordSpell.js`，规则统一为「非字母字符（空格、连字符、句号、问号…）按答案原样带出，只有字母/撇号吃孩子输入」；后端判分本来就保留标点，所以对得上
- 加数据驱动回归 `scripts/check_word_spell.mjs`：753 条词库（含标点 58 条）逐条断言「只敲字母 → 拼出答案」，现在 0 条错

## 2026-09-16 v0.3.51 - 修概览页空白（读 null 抛错）＋页面级错误边界

- 根因（v0.3.47 引入）：没设共同目标时后端返回 `family_goal:{goal:null,progress:null,by_kid:[]}`——外层对象为真，概览页只判了外层，接着读 `null.metric_label` 抛错，Vue 把整块子树卸载 → 侧栏还在、主区空白，且没有任何提示
- 修：只有「真的有一个目标」（`goal` 与 `progress` 都在）才当有；并补一条**真实形状**的 SSR 用例，把这种 payload 钉住
- 新增页面级错误边界 `PageBoundary.vue`：任何页面在渲染期抛错都只显示「这一页渲染出错了 ＋ 原因 ＋ 重试」，不再静默空白（浏览器实测过；SSR 验不了这个机制）

## 2026-09-16 v0.3.50 - 页面数据没加载上不再静默空白

- 工作台按页加载（pack）里某个接口失败时：以前 Promise 抛出去、页面只剩空壳，现在底下会出一条「这一页的数据没加载上 + 具体哪个包失败 + 重试」，一眼就知道断在哪，点重试重新拉
- 一个包失败不再影响同一页其它包

## 2026-09-16 v0.3.49 - 共同目标出问题不拖垮页面

- B4 是最新加的可选功能，它那部分一旦抛错不能把「概览」页的核心接口（`/api/admin/family-today`）或孩子端 payload 带崩：目标进度读取、孩子端那行、打卡/签到后的结算三处都改成失败即降级（页面照常渲染，打卡照常成功）
- 新增用例 `test_goal_error_does_not_break_core_pages`：把目标进度搞成必抛，断言 `family-today` 与 `/api/tasks` 仍 200、打卡仍成功

## 2026-09-16 v0.3.48 - 家庭共同目标幂等索引补上 family_goal

- 迁移 043 先 drop 再建 ux_ledger_once，生产库才能把 family_goal 纳入一次性流水幂等

## 2026-09-16 v0.3.47 - 家庭共同目标（B4）

- 家长端「概览」新增一段：设一个全家共同目标（本周累计完成卡数 / 运动次数 / 签到天数），达标每人发 0–20 阳光（默认 5，家长定）
- 孩子端「今天怎么做」顶部显示「🎯 全家还差 N 张 / 次 / 天」，达标后显示「全家达成目标啦」
- 同时只 1 个进行中目标；每人只发一次（靠流水幂等索引兜底，不靠判断）；过期或家长手动关闭都不扣分、不补发
- 口径：完成卡数 = 当日 completed（取消不算）；运动次数 = 体育类每日任务完成次数（课本任务不算）；签到天数 = 各娃之和（同一个孩子同日重复签到只算一次）
- 迁移 043：family_goals 表 + 「单活跃目标」唯一索引 + family_goal 纳入一次性流水幂等

## 2026-09-16 v0.3.46 - 孩子端样式尾巴清理（内部重整）

- App.vue 全局样式块 806 行 → 609 行：删掉 72 条没有任何使用者的死规则（旧版储蓄所 / 领取 / 重命名那几套），页面专属样式 82 条搬进对应组件的 scoped（储蓄所 34、秘密基地 23、伙伴抽屉 12、阳光页 11、工作台阳光页 2），断点跟着一起走
- 搬迁门槛是「全仓别处没有任何规则的选择器提到这些 class」——优先级 +1（scoped 属性）没有对手，行为可证明不变；App.vue 模板或脚本还在用、以及有竞争定义的规则一律留全局
- 家长工作台 scoped 里 2 条死规则（`.a-term`）删掉
- 校验：改前 454 条规则 = 改后 + 搬走 82 + 删掉 72，每条搬走的规则声明逐字一致（脚本比对）

## 2026-09-16 v0.3.45 - 体育不再默认退出孩子端侧栏

- 体育以前是硬编码「只在今日推荐、不进侧栏」（v0.2.68）；现在改由家长端「孩子端显示学科」开关控制：开关开着且体育有内容（它的每日任务）就进侧栏
- 围棋保持原样（只有对弈，仍放今日推荐）；道法仍按 v0.2.27 的默认隐藏，家长可在开关里打开

## 2026-09-16 v0.3.44 - 家长工作台已学到页拆成子组件（内部重整）

- 已学到（已学到哪一课 / 孩子端显示学科 / 进度锁 / 图鉴与基地，侧栏同一项）搬进 `AdminCursor.vue`；数据仍由工作台按页加载持有，界面与交互不变
- 这一页四个开关都是即时保存，动作仍由工作台执行（含切孩子在途请求的守卫），子组件只 emit，不需要脏条那三个方法
- 已学到相关样式（含窄屏那行）移到全局底座；壳里 `SUBJECT_ORDER` 导入随之删掉
- 家长工作台模板里最后一段内联页面清空：8 个页面现在都是组件标签

## 2026-09-16 v0.3.43 - 家长工作台清掉没人用的导入（内部重整）

- 工作台里 7 个随页面搬走就没再用到的导入删掉（Sun / Check / tagHelp / n1 / isTimeMetric / formatDuration / formatMetricValue），界面与交互不变

## 2026-09-16 v0.3.42 - 修任务页四条规则设置行的样式（v0.3.36 漏搬）

- 「单元测试」里四条规则设置行（连续低分次数 / 低于多少分算低 / 完成量少几成算下滑 / 连击断几天再提）的 `.a-item` 样式只在工作台的 scoped 样式里，多根子组件不会继承父组件的 scopeId → 这几行一直没套上样式；现在移到全局底座
- 顺手删掉早就没人用的 `.a-subject-h` / `.w-name` / `.w-cat`

## 2026-09-16 v0.3.41 - 家长工作台家庭页拆成子组件（内部重整）

- 家庭（孩子账号 / 家长成员 / 邀请码 / 家长密码，侧栏同一项）的界面与本页表单搬进 `AdminKids.vue`，数据仍由工作台按页加载持有；界面与交互不变
- 未保存提示的「保存 / 放弃」走子组件暴露的 isAddDirty / discardAdd / saveCurrentEdit；删掉当前选中的孩子后由工作台清掉选择
- 家庭页样式（孩子卡、成员行、邀请码、家长密码表单，含窄屏收单列那条覆盖）移到全局底座

## 2026-09-16 v0.3.40 - 家长工作台阳光页拆成子组件（内部重整）

- 阳光（商店 / 银行 / 等级 / 扣分，侧栏同一项）的界面与提交搬进 `AdminShop.vue`，数据仍由工作台的 shop 包持有；界面与交互不变
- 未保存提示的「保存 / 放弃」改走子组件暴露的 isAddDirty / discardAdd / saveCurrentEdit，奖励与等级的行内编辑也由它保存
- 扣分 chip 与净扣样式移到全局底座；壳里随搬走变死的 rankIcon / Star 导入删掉

## 2026-09-15 v0.3.39 - 家长工作台今日复习拆成子组件（内部重整）

- 今日复习的科目筛选与列表搬进 AdminReview.vue；会了/还不熟/已掌握仍由工作台执行
- 复习页样式（含任务页的「新加入的考点从这天开始」）移到全局底座，子组件也能用

## 2026-09-15 v0.3.38 - 家长工作台兑换审批拆成子组件（内部重整）

- 兑换审批的筛选与列表搬进 AdminApprove.vue；同意/拒绝/兑现仍由工作台执行，界面与交互不变
- 状态徽章样式移到全局底座，审批页自己也能用

## 2026-09-15 v0.3.37 - 家长工作台清理任务页残留（内部重整）

- 任务页搬进子组件后，壳里没人用的表单态、提交函数、考点卡辅助等已删掉（约 90 行）

## 2026-09-15 v0.3.36 - 家长工作台任务页拆成子组件（内部重整）

- 任务与考点 / 每日任务 / 单元测试三段搬进 AdminTasks.vue，本页表单与提交一并搬走
- 数据仍由工作台按页加载持有；未保存提示（脏条）改为通过子组件暴露的三个方法工作

## 2026-09-15 v0.3.35 - 家长工作台编辑会话抽成模块（内部重整）

- 商店、等级、家长任务、每日任务、孩子账号共用的「改 / 取消 / 保存」会话搬到 adminEdit.js；界面与交互不变

## 2026-09-15 v0.3.34 - 家长工作台总览拆成子组件（内部重整）

- 总览界面搬进 `AdminInsights.vue`，该页专属的计算一并搬走；界面与交互不变
- 总览数据仍由工作台按页加载持有，切走再回来不会重复请求

## 2026-09-15 v0.3.33 - 家长工作台单词页拆成子组件（内部重整）

- 英语单词页的界面搬进 `AdminWords.vue`，数据与请求搬进 `adminWords.js`；界面、保存时机和交互不变
- 侧栏切走再回到单词页不会重复请求单词接口（沿用 v0.3.23 的按页加载）

## 2026-09-15 v0.3.32 - 家长工作台样式底座（内部重整）

- 家长工作台页面样式抽到全局 `adminBase.css`，每条规则都限定在家长工作台内，为下一步按页拆子组件做准备
- 只搬不改：界面、保存时机和交互与 v0.3.31 完全一致

## 2026-09-15 v0.3.31 - 看到第一题也算看过

- 打开题库看到第一题再离开，今日闸门也会打开；只停留在过期结束页仍不算

## 2026-09-15 v0.3.30 - 看题也能打开今日闸门

- 题库页看过至少一题再离开，今日闸门才会打开；只打开页面立刻返回不算
- 练习或模拟考完成仍然能打开闸门

## 2026-09-15 v0.3.29 - 孩子端题库闸门在打开打卡前拦住

- 今日卡和科目卡都显示「需先完成今日题库练习」，有链接才出「去做」
- 没做完题库时点打卡会提示并跳转，不再先弹出打卡窗

## 2026-09-15 v0.3.28 - 每日任务可跳转题库，勾选需先做题

- 家长可给每日任务填「去哪做」和「必须先完成今日题库」；取消或放弃会还原勾选，不会把闸门带到下次新增
- 题库练习会上报进度；没做完题库时，勾了闸门的任务不能打卡领阳光

## 2026-09-15 v0.3.26 - 家长工作台未保存提示

- 改了一行、填了新增、或改了周目标 / 成绩档位 / 银行目标时，底部出现「有未保存的修改」
- 切页、换孩子、回到孩子端会先确认；确定就丢掉这些修改。开关仍然马上保存，不算未保存

## 2026-09-15 v0.3.25 - 家长工作台同时只编一行

- 商店、成长等级、家庭每日任务、家长自定义任务和孩子账号共用一套「改 / 取消」
- 整个工作台同时只能编一行；点另一行会先还原上一行未保存的修改
- 保存成功才退出编辑；保存失败还停在编辑态，刷新当前页会清掉编辑会话

## 2026-09-14 v0.3.24 - 家长侧栏收到 8 项

- 侧栏改为总览、今日复习、兑换审批、任务、英语单词、已学到、阳光、家庭
- 每日任务和单元测试并进任务页；商店、银行、等级、扣分并进阳光页；孩子、成员、邀请、密码并进家庭页
- 旧入口「阳光图鉴」不再单独占一项，开关仍在已学到；孩子账号默认只读，点「改」再编辑，取消会还原未保存的修改

## 2026-09-14 v0.3.23 - 家长工作台按页加载

- 打开哪一页才拉哪一页的数据；商店、银行、单词不再跟着密码页一起请求
- 换孩子丢掉进行中的请求并只重拉跟这个孩子有关的数据；保存后只刷新当前页

## 2026-09-14 v0.3.22 - 家长工作台记住上次打开的页

- 刷新或再进工作台回到上次那一项；旧的「周报」和未知值会写成总览
- 只认当前侧栏里的页；加载失败也不会把人甩回总览

## 2026-09-14 v0.3.21 - 商店等级与每日任务先看再改

- 商店、成长等级、家庭每日任务默认只读，点「改」再编辑；系统内置每日任务仍只读
- 新增每日任务表单默认收起，保存成功后自动收起；邀请码保护改成和其他页一样的开关

## 2026-09-14 v0.3.20 - 家长设置位置整理

- 本周目标移到总览「本周阳光」区域，孩子账号页回到账号资料管理
- 图鉴与秘密基地开关移到「已学到」页，原入口保留跳转提示

## 2026-09-14 v0.3.19 - 周目标与图鉴搬家

- 本周攒阳光目标移到总览「本周阳光」下，孩子账号只剩资料与密码
- 图鉴/秘密基地开关并入「已学到」；侧栏「阳光图鉴」点击后跳转过去

## 2026-09-14 v0.3.18 - 任务与考点细节折叠

- 新增家长任务表单默认收起，教材任务按单元默认收起
- 家长自定义任务继续直接显示并可编辑，教材任务仍保持只读
- 复习开始日期提示移到科目筛选下方

## 2026-09-14 v0.3.17 - 单元测试先录分

- 单元测试默认先录入成绩和历史；成绩档位与诊断阈值收进同一处规则折叠
- 总览拿掉的阈值入口接到测试页，录分预览阳光仍在表单里

## 2026-09-14 v0.3.16 - 任务与考点默认收起

- 新增家长任务表单默认收起，点击后再展开
- 教材任务默认按单元收起，家长任务继续直接显示并可编辑
- 复习开始日期提示移到科目筛选下方，操作顺序更清楚

## 2026-09-14 v0.3.15 - 审批与银行取出先看未完成

- 兑换审批默认「待同意」，可切待兑现 / 已结束；空状态按段提示
- 银行取出申请默认只列待批准，已处理收进「看已处理」；补结算仅创建者可见

## 2026-09-14 v0.3.14 - 家长单词页分段

- 英语单词拆成今天、怎么练、词书三段；第一屏只看进度和错词
- 近 7 日和导入单词默认收起，朗读与节奏的保存时机不变

## 2026-09-14 v0.3.13 - 家长总览详情折叠

- 总览默认聚焦问候、待办、打卡、阳光和盯点，减少首屏信息量
- 体测、趋势图、每日柱、各科统计和约定明细收进「本周详情」

## 2026-09-14 v0.3.12 - 家长总览减负

- 总览默认只留问候、待办、打卡、3 格阳光、盯点和多孩卡片
- 体测、近 4 周折线、每日柱和各科进「本周详情」；诊断阈值入口从总览拿掉

## 2026-09-14 v0.3.11 - 家长端文案与侧栏图标修正

- 单元测试改用清单图标，扣分改用减号图标，避免两个入口视觉混淆
- 今日复习、测试历史和隐藏科目提示统一显示科目中文名
- 扣分记录主标题显示原因，备注移到副文案

## 2026-09-14 v0.3.10 - 家长端文案与侧栏图标

- 单元测试和扣分侧栏图标分开，不再都用同一份文件图标
- 今日复习、测试历史、隐藏科目提示改用科目中文名
- 扣分列表主标题改为原因，备注放到副文案

## 2026-09-14 v0.3.09 - 单词快捷方案回显修正

- 快捷方案仅在自定义复习范围模式下显示
- 根据当前勾选的系统词书自动回显三年级、四年级或三四年级方案；手动组合不匹配时显示占位提示

## 2026-09-14 v0.3.08 - 单词快捷方案可回显

- 快捷方案只在「自定义范围（补基础）」时出现，不再和「跟随当前词书」叠在一起
- 下拉按当前勾选的系统词书回显「三年级 / 四年级 / 三、四年级」方案；手改勾选对不上时回到占位

## 2026-09-14 v0.3.07 - 家长端词库范围显示修正

- 修复家长端 `hiddenSubjects` 未声明导致工作台加载失败的问题
- 自定义复习范围卡片样式在桌面和移动端都生效

## 2026-09-14 v0.3.06 - 家长端加载与单词范围样式

- 补上家长端 `hiddenSubjects` 声明，避免进工作台加载中断
- 单词自定义范围的分组样式从窄屏媒体查询里拿出，桌面也能按学期分卡片

## 2026-09-14 v0.3.05 - 单词复习范围快捷方案

- 家长端增加「三年级基础」「四年级基础」「三、四年级补基础」快捷方案
- 方案按系统词书排序自动选择复习范围，并把范围首本设为新词起始书

## 2026-09-14 v0.3.04 - 单词复习范围可配置

- 家长端可选择「当前词书」或「自定义范围（补基础）」；自定义范围支持按年级选择系统词书
- 自定义范围按选定词书顺序取到期词和新词，不受英语「已学到」游标限制；当前模式保持原有行为
- 范围设置会过滤无效词书，系统词书仍只读，家庭词书和原有 SRS 隔离不变

## 2026-09-13 v0.3.03 - 多学段英语词库与词条元数据

- 系统词库扩展到三至五年级，共 42 个单元，补充页码、词条类型、核心标记和教材来源信息
- 新增 `039_word_metadata` 迁移；系统词书仍只读，专名/节日名等非练习词不进入新词练习
- 单词 session 返回 `page`、`entry_type`、`core` 元数据，保留家庭词书隔离和原有 SRS 流程

## 2026-09-13 v0.3.02 - 阳光页目标条微调

- 改目标时输入框放到进度条上面；进度条宽度有短过渡，系统减少动效时关掉
- 保存本周目标时直接改 summary，不再整份替换

## 2026-09-12 v0.3.01 - 阳光页样式归位

- 阳光页 `sun-*` 样式迁到 SunshinePage scoped；旧版 sun-lead/sun-src/sun-log 死样式删掉
- 家长银行仍用 sun-hero/sun-box；SunshinePage 不再接收 dailyTodo/studyNext props

## 2026-09-12 v0.3.00 - 阳光页柱子动画和上周对比

- 柱子高度有短过渡，系统减少动效时关掉；浅色柱是上周同一天
- 周合计数字滚动；全 0 周提示「完成第一个任务，这里就会亮起来」；柱子有朗读标签


## 2026-09-12 v0.2.99 - 本周攒阳光目标

- 孩子和家长都能设本周目标（默认 50，0=关掉）；进度按自然周 weekIn，存银行计入攒到
- 阳光页显示本周目标进度，达标变绿，超额显示超标；家长孩子都能改


## 2026-09-12 v0.2.98 - 阳光页可切周、点天看明细

- 趋势区加上一周/下一周按钮，显示自然周范围；点某天柱子展开当日口袋流水
- 趋势下方列出本周兑换；summary 带 redemptions，`/api/ledger?date=` 返回当天明细


## 2026-09-12 v0.2.97 - 阳光页改读自然周聚合

- 新增 `GET /api/ledger/summary`：按周一到周日聚合口袋阳光，存银行算攒到，约定扣分不进柱
- 「我的阳光」不再用最近 60 条流水现算周趋势，避免活跃孩子把 14 天窗口截断
- 今日约定改读 summary 的 `penalty_today`



## 2026-09-12 v0.2.96 - 公用 UI 样式归位

- 弹窗、按钮、成就墙和胶囊样式移到 `ui.css` / 对应组件，App.vue 继续变薄
- 保持页面结构和视觉不变
## 2026-09-12 v0.2.95 - 基地、胶囊、阳光页和单词练习组件化

- 秘密基地、时间胶囊、阳光统计、单词练习移到独立组件
- 共享状态集中到 `store.js`，App.vue 进一步变薄
- 保持登录、打卡、TTS、图鉴、银行和胶囊流程不变

## 2026-09-12 v0.2.94 - 围棋打卡提示补回

- 每日打卡组件化后补回围棋「赢 1 局才给阳光」提示
- 其他每日任务也明确显示本次打卡阳光

## 2026-09-12 v0.2.93 - 家长退出后台回到孩子登录页

- 家长点击「回到孩子端」时清理家长会话，回到孩子登录页，避免角色状态残留导致白屏

## 2026-09-12 v0.2.91 - 修复拆分后的主区域标签

- 补回 App.vue 主内容区闭合标签，修复组件拆分后生产构建失败导致的白屏
- 生产构建和 smoke 已通过

## 2026-09-12 v0.2.90 - 修复组件化白屏

- 清理 App.vue 中已迁移的伙伴定时器卸载引用
- 登录组件补上共享 toast 状态，确保错误提示和首次改密页面正常渲染
- 生产构建和前端 smoke 已通过

## 2026-09-12 v0.2.89 - 每日打卡和伙伴抽屉组件化

- 每日指标/分秒百分秒/围棋录入移到 `DailyDialog.vue`
- 伙伴抽屉、进化庆祝和升级庆祝移到 `CompanionDrawer.vue`
- 保持打卡奖励、庆祝、TTS 和秘密基地入口行为不变

## 2026-09-12 v0.2.88 - 登录页独立成组件

- 登录、注册、邀请码、找回密码和首次改密移到 `LoginScreen.vue`
- 保持孩子入口、家长后台和找回码流程不变
- smoke 检查同步支持组件化登录页

## 2026-09-12 v0.2.87 - 前端组件继续拆开

- 趋势图、成就墙、兑换商店各自成为独立组件，App.vue 只保留调用和跨块状态
- 成就徽标、toast、彩纸动效改由共享模块提供
- 先保持功能和视觉不变，继续为后续拆分铺路

## 2026-09-12 v0.2.86 - 前端共享状态先落地

- 抽出孩子端共享状态 `store.js` 和时间/指标格式函数 `format.js`
- 家长端与孩子端共用计时展示逻辑，先保持页面行为和视觉不变

## 2026-09-12 v0.2.85 - 孩子密码可以自动生成

- 家长新增孩子时密码可留空，系统随机生成 6 位合规密码
- 密码只在创建成功的弹窗里显示一次；也可以继续自己填写密码

## 2026-09-12 v0.2.84 - 并发和审计再收紧

- 直连公网时不再信任伪造的代理 IP 头；邀请码并发只能成功一次
- 时间胶囊、取阳光、兑换审批和管理端保存操作加强防重复
- 拒绝兑换改为保留记录，审计里能看到「已拒绝」

## 2026-09-12 v0.2.83 - 利息错过会补结算

- 活期利息周六没结也不丢：下次打开储蓄所按最近到期日补一笔，同一周期不会发两次
- 连击、周报、昨晚报按上海时间算日子；家长后台加载失败不再整页空白
- 音效文件缺失时改用提示音；孩子端刷新不会被更早的请求盖掉

## 2026-09-11 v0.2.81 - 储蓄所晚上八点打烊

- 阳光储蓄所每天 8:00 到 20:00（上海时间）营业；打烊后不能存、取、开存单或提前支取
- 门头改成「已打烊」，柜台写下班了；金库和存折还能看

## 2026-09-11 v0.2.80 - 储蓄所活期定存说清楚

- 存多久六个按钮一样高、排成格子，活期也写「随时可取」
- 右上角活期利率改成只算没锁进存单的阳光，和定存不抢同一句话

## 2026-09-11 v0.2.79 - 打卡只开到晚上九点

- 每天 7:00 到 21:00（上海时间）才能签到、打卡、学单词和完成学习任务
- 打烊后侧栏会写「现在打烊」，点任务会提示太早或已经打烊；家长取消打卡不受这个限制

## 2026-09-11 v0.2.78 - 储蓄所能开定存单

- 孩子存阳光时可自选 7 / 10 / 15 / 30 / 60 天，到期一次性结息：2%、3%、5%、8%、12%
- 定存锁定，活期才能取出；提前支取按已过天数打五折，当天存当天取没有利息
- 家长后台原来的利率只管活期，定存锁定的阳光不参与周结息

## 2026-09-11 v0.2.77 - 时间胶囊能多选日子

- 开封时间从两档改成：一周后、一个月后、下学期开学、一年后、连打 30 天，也可以自己从日历里选（最早明天，最远三年）
- 三个问题下面有例句，点一下就能填进去，输入框也有提示

## 2026-09-11 v0.2.76 - 风车月亮和信封再对齐

- 天台风车杆放到叶片后面，转起来不会盖住叶片
- 云上月亮光晕对上底图那一轮；树屋信封再往木屋上挪一点

## 2026-09-11 v0.2.75 - 昨晚报改到基地里

- 昨晚报改成进秘密基地或图鉴才出现，不再挡住今日任务
- 到期复习空了，云上那轮月亮加一圈光晕，不再另画一颗月

## 2026-09-11 v0.2.74 - 竖屏三页收一收

- 我的阳光、秘密基地、阳光储蓄所在手机和平板竖屏改成单列：柜台在上金库在下，缺口更好点，场景切换按钮更大
- 天台风车杆和叶片重新切开，转起来不会整根糊在一起

## 2026-09-11 v0.2.73 - 竖屏顶栏收短

- 手机和平板竖屏：顶栏改成一行名字+胶囊，底下一条升级进度，不再叠成三层

## 2026-09-11 v0.2.72 - 时间胶囊改成信封

- 树屋木屋上挂一封信，不再是地上的箱子；图鉴预览里也能点开
- 天台风车仍要今天打过卡才出现

## 2026-09-11 v0.2.71 - 时间胶囊第一刀

- 秘密基地树屋多一只免费箱子：给未来的自己写三句，选下学期开学或再连打 30 天再拆
- 封着时看不到正文；到期今日推荐出一张卡，拆开能对照那时的你和现在的你
- 不发阳光；同时只能有一封在路上

## 2026-09-11 v0.2.70 - 储蓄所换成暖色卡片

- 门头、柜台、金库改成和整站一样的暖色卡片，去掉木牌铁柜
- 侧栏入口改成「阳光储蓄所」

## 2026-09-11 v0.2.69 - 阳光储蓄所

- 孩子端银行改成一间储蓄所：门头、柜台、保险柜
- 口袋在柜台托盘，金库在柜门大数字；取出申请变成「等家长开门」等候牌
- 点保险柜看存折；小心愿贴在柜门上

## 2026-09-11 v0.2.68 - 围棋赢一局才给阳光

- 围棋对弈只看「赢了几局」：填 1 或更多才给阳光；赢 0 局（空着也算 0）可以记下，但不给阳光；输了几局不影响
- 打卡卡片和弹窗会写明这条规则；按钮在没赢时改成「记下对局，这次没有阳光」
- 侧栏不再显示体育、围棋，这两科只留在今日推荐

## 2026-09-11 v0.2.67 - 打卡用时按秒表录入

- 跳绳、口算等计时改成秒表三段：分 : 秒 . 百分秒（秒后面两位，例如 0'23.45"）
- 打完两位秒会跳到百分秒；趋势和个人纪录也按两位小数显示

## 2026-09-10 v0.2.64 - 复习看一眼只一次

- 到期复习每个词只能「忘了，看一眼」一次，两秒后收回；用完按钮消失
- 新词点「去默写」后不能再回看，写错才会显示正确答案

## 2026-09-10 v0.2.63 - 升级按净增阳光

- 升级进度按净增计算：取消打卡、约定扣分会减少「再得多少升级」
- 商店兑换仍不掉级

## 2026-09-10 v0.2.38 - CI/CD 自动构建

- GitHub Actions 自动构建 APK 并部署到 VPS
- 自动创建 GitHub Release，APK 文件带版本号
- 后端增加 `/api/version` 和 `/api/apk/latest` 接口
- 平板可通过 GitHub Releases 或 VPS 下载最新 APK

## 2026-09-10 v0.2.37 - P0 UI 改进：音效与鼓励

- 打卡时有音效（叮声+硬币声）和手机震动反馈
- 10 种随机鼓励文案，根据连续打卡天数和时段智能变化
- 圆圈打卡 0.3 秒填充动画，悬停放大+旋转
- 桌面端卡片间距增加到 18px，视觉更舒展
- 升级、伙伴成长有专属音效和震动
- 支持音效开关和音量控制

## 2026-09-10 v0.2.36 - 攒和兑换分清

- 趋势里的「花」只算商店兑换；取消打卡收回、约定扣分都不算花
- 当天取消会把攒到的冲掉，空柱才表示真没攒到

## 2026-09-10 v0.2.35 - 我的阳光看缺口

- 不再重复顶栏的口袋和连打；改成还没做好、这周攒/花趋势、五条途径和上周对比
- 缺口能点：打卡、单词、复习、课文会跳到对应地方

## 2026-09-10 v0.2.34 - 我的阳光更好看

- 口袋、攒过、连打改成和大卡片一样的存折排法；等级进度、这周每天、来源和流水都收进卡片里
- 点右上角等级徽章还能打开成长地图

## 2026-09-10 v0.2.33 - 阳光银行更好看

- 孩子端银行页改成存折卡片：银行存款、口袋余额、目标进度、存取按钮和流水分开排
- 利息、申请状态和进出账更清楚，没有目标时也有一句提示

## 2026-09-10 v0.2.32 - 玩具店和星尘

- 树屋小旗不再按旧连击补挂，要有「拿到了」才出现
- 玩具店改成按钮弹窗，不挤在基地画面下面
- 秘密基地和图鉴标题旁显示还有多少星尘

## 2026-09-10 v0.2.31 - 秘密基地接上三个角落

- 侧栏「秘密基地」在图鉴和基地都开着时，显示天台/树屋/云上，不再一直写建设中
- 只关图鉴时，基地页才显示建设中

## 2026-09-10 v0.2.30 - 银行利息功能

- 家长后台可配置银行利息：周期（每周/每两周/每月）、利率（0%–10%）、起存点（0/10/20/50/100）
- 按周期自动结算利息，计算日均余额，低于起存点不计息
- 孩子端银行页显示利息提示，流水中显示利息记录
- 家长端可手动触发结算测试，查看上次结算日期
- 迁移 033 添加利息配置键约定

## 2026-09-10 v0.2.29 - 阳光图鉴

- 连击宝箱会孵出阳光精灵，进图鉴；重复的变成星尘，可以升星、买小玩具
- 秘密基地三个角落（天台/树屋/云上）；关图鉴时基地显示「建设中」，不把基地一起关掉
- 家长后台可以分开开关「阳光图鉴」和「秘密基地」

## 2026-09-10 v0.2.28 - 阳光银行第一稿

- 家长后台可按孩子开关阳光银行、设置一个存钱目标、审批取出申请
- 孩子可以存入阳光，取出需家长同意；银行阳光不能直接兑换商店奖励
- 银行与口袋分账，存取都留下可追溯流水

## 2026-09-10 v0.2.27 - 学科可以关掉

- 家长后台「已学到」里可以开关孩子端显示哪些学科
- 道法默认关掉，侧栏和今日推荐都看不到；任务还在，随时开回来

## 2026-09-10 v0.2.26 - 默写打在短横上

- 默写不再出现下面的输入框，字母直接显示在短横上；点短横出键盘
- 安卓壳改用系统朗读，网页里点听读音经常没声

## 2026-09-10 v0.2.25 - 我的阳光更好认

- 口袋/攒过/连续打卡加大图标；阳光从哪来改成三张卡；流水每条带图标

## 2026-09-10 v0.2.24 - 建设中居中

- 秘密基地、阳光银行的「建设中」卡片在右侧页面正中

## 2026-09-09 v0.2.23 - 我的阳光

- 侧栏「最近阳光」改成「我的阳光」，下面加上秘密基地、阳光银行
- 右侧看口袋余额、近 7 天进出、阳光从哪来；秘密基地和阳光银行先显示建设中

## 2026-09-09 v0.2.22 - 单词拆成复习和新词

- 每日打卡里「今日复习」在前、「今日新词」在后；点哪张只练那一段
- 还是同一场练习、一次阳光；两段都写完才能收下阳光

## 2026-09-09 v0.2.21 - 取消/开箱连点只记一笔

- 取消打卡、开箱、撤回扣分、批准兑换、删测试冲正：同一笔流水不能再写第二次
- 孩子端打卡/取消/开箱加提交锁；伙伴长大确认不会把阶段往回拨
- 猜邀请码失败仍限流，成功加入家庭不占爆破次数

## 2026-09-09 v0.2.20 - 用时改成分+秒

- 跳绳 100 下、口算用时等「秒/分钟」指标，打卡时填「几分几秒」
- 趋势和个人纪录显示成 1'05\"，旧记录不用重填

## 2026-09-09 v0.2.19 - 词书去掉拼读填充和整句

- 各单元不再塞 class/green/how much 这类拼读例词或语法整句；只留本课主题核心词
- 频率副词仍在 Unit 5；Unit 1 不练 always/usually

## 2026-09-09 v0.2.18 - Unit 1 频率副词挪到 Unit 5

- always / usually / often / sometimes / never 从 Unit 1 挪到 Unit 5（周末活动语法点）

## 2026-09-09 v0.2.17 - 平板听读音

- 点「听读音」会选一台设备上真实存在的英语语音，不再死盯英式标签
- Android 壳允许 WebView 在点喇叭后出声；语音列表晚到时再试一次

## 2026-09-09 v0.2.16 - 主屏阳光精灵

- 「今天」顶栏字母头像换成阳光芽：蛋 → 芽 → 苗 → 花，只看累计阳光
- 连击满 7/14/30 天加光晕，不改阶段；点开可起名；长大时庆祝一次（老用户第一次打开不弹）

## 2026-09-09 v0.2.15 - 单词并进每日打卡

- 今日推荐不再单列「今日单词」；单词卡排在英语打卡前面（自然拼读旁）
- 每日打卡按语文 → 数学 → 英语 → 体育 → 围棋排序

## 2026-09-09 v0.2.14 - 五上英语词书 Unit 1–8

- 系统词书补齐译林五上 Unit 2–8 和两个 Project（核心词，页码仍待对照课本 Word lists）
- 家长把英语「已学到」拨到该课，才能把那一本设成当前新词词书；旧单元到期词还会出现

## 2026-09-09 v0.2.13 - 单词入口改成打卡卡

- 英语科目「每日打卡」里，「今日单词」和自然拼读同一排卡片；点开仍是背默遮罩
- 今日推荐打卡和本课之间也用同一张卡，不另开页面

## 2026-09-09 v0.2.12 - 家长端英语单词

- 学习组新增「英语单词」：开关、词书、导入、高频错词、朗读、近 7 日完成次数和首轮正确率
- 系统词书只读（单元/词数/来源版本）；家庭词书可建、导入、改名、软删除
- 节奏（每天几个词、阳光）只影响明天的新练习；朗读开关马上生效

## 2026-09-09 v0.2.11 - 孩子端单词遮罩

- 今日推荐在打卡和本课之间加入「今日单词」；英语科目页也有入口
- 遮罩：新词先看词，到期词直接默写；看词有音标和听读音（设备朗读）
- 写错再写一次不计分；练完点「收下阳光」才发奖，关掉遮罩不算放弃

## 2026-09-09 v0.2.10 - 单词 session 与阳光

- 孩子端 `today` / `start` / `study` / `spell` / `complete`：当天题单固定，刷新不重抽
- 默写走五档间隔；错词当天只再写一次，不改档位、不扣阳光
- 完成时按创建时快照发 `word_daily` / `word_perfect`，同一 session 各最多一笔

## 2026-09-09 v0.2.9 - 英语 Unit 1 词表

- 译林五上 Unit 1 Good habits 试点 24 个核心词入库（待对照课本 Word lists 补页码）
- 家长可建家庭词书、TSV/CSV 导入；系统词书只读
- 单词开关和节奏写在每个孩子的设置里，默认关；孩子端背默下一刀再接

## 2026-09-09 v0.2.8 - 成就段位与稀有度

- 成就墙按里程碑/学科/坚持/阳光分组，青铜到传说四档边框
- 阳光、坚持、学科三条段位链；拿到徽章记下时间，未看过的会弹窗
- 新增早鸟、复习达人、单词首秀、满分一周、坚持两月（单词要等词库模块才会点亮）

## 2026-09-08 v0.2.3 - 南通一二年级英语用译林一年级起点

- 南通一年级就开英语，课本是译林《英语》（一年级起点 / 一起），不是人教新起点
- 补 1A–2B 八单元目录；一、二年级上册按课文写成可勾选考点
- 去掉登录和家长页的副标题、说明段落
- 今日推荐「每日打卡」标题下按年级轮换名句/歇后语

## 2026-09-08 v0.2.2 - 1–4 年级上册单元考点

- 一年级到四年级上册语数英（三年级起）科道法按课文写成可勾选考点
- 入学教育、数学游戏分享、期末复习不标；五上原 101 条映射不变

## 2026-09-08 v0.2.1 - 总览收掉重复周报

- 侧栏去掉「本周周报」；赚/花/净增、已掌握、孩子对比并进总览
- 三张图只保留一份，不再两页各画一遍

## 2026-09-08 v0.2.0 - 给别的家庭用的安装包

- 登录默认孩子入口，「我是家长」才出现注册/邀请码/忘记密码
- 新家必须先加第一个孩子；注册时发一次性找回码（只显示一次）
- 一家最多 5 个孩子；登录/注册限流写入 SQLite（同 IP 24 小时最多 3 家）
- 安卓壳可长按改服务器地址，允许 HTTP；版本号跟 VERSION
- 对外说明书见 FAMILY.md

## 2026-09-08 v0.1.44 - 创建者权限、备份还原、文档

- 普通家长看不到删孩子/删成员/邀请码；创建者可以把身份交给别人
- `restore_db.sh` 列出备份并还原；`install_backup_cron.sh` 每天备两次
- README / OPS / TODO 按 SQLite-only 现状改过，不再写 PostgreSQL 和过期密码

## 2026-09-08 v0.1.43 - 顶栏与各科条

- 顶栏去掉 SUNSHINE /「家长 ·」，版本号缩小
- 本周各科横条按阳光长短，不再按次数（次数相同就会一样长）

## 2026-09-08 v0.1.42 - 去掉体测说明，每天也用净增

- 去掉体测三项的解释文案
- 本周每天柱状图与四周图同一口径：净增

## 2026-09-08 v0.1.41 - 总览提示与净增

- 没有复习/审批时不再出现「先看某某的体测」
- 近 4 周图改为净增（赚到 − 兑换 − 约定），不再只加正数

## 2026-09-08 v0.1.40 - 体测白名单

- 「体测数值」只收录有年级达标线的三项（跳绳、仰卧起坐、坐位体前屈），不再按「有数字」或「体育科」一锅端

## 2026-09-08 v0.1.39 - 体测数值不含口算

- 总览「体测数值」只显示体育项（跳绳、体前屈等），口算等有数字的打卡不再混进去

## 2026-09-08 v0.1.38 - 阳光提示改到顶栏下方

- 打卡/完成任务的提示不再贴在页面底部（原先压着底栏），改到顶栏下面、靠近阳光余额

## 2026-09-08 v0.1.37 - 孩子端约定显示

- 扣分在孩子端叫「约定」，不出现惩罚字眼
- 今日推荐有记下时给一句说明：少了多少阳光、等级不掉、做好还能攒回来
- 流水里用琥珀色，孩子不能自己撤回

## 2026-09-08 v0.1.36 - 总览打卡与体测分析

- 总览增加今日打卡清单（已打 / 还没做）
- 有数值的体育项（跳绳、体前屈等）对照达标线，并画历次折线和个人最好

## 2026-09-08 v0.1.35 - 总览图表、已学到对齐、扣分与账号

- 总览去掉四个入口卡，改用现有周报数据：近 4 周折线、本周柱、各科条；顶部先提示该处理的事
- 「已学到」每科同一行宽下拉，不再长短不一
- 扣分改为金额/原因胶囊，提交前确认；默认关闭说清楚用途
- 孩子账号两列对齐；改密码要确认；保存和删除分开写清楚

## 2026-09-08 v0.1.34 - 单孩总览与登录密码说明

- 登录页不再写「孩子 4 位密码」，改为孩子至少 6 位、家长至少 8 位
- 只有一个孩子时：不出现「切换孩子」，顶栏只显示名字，总览用「今天」而不是「孩子们」

## 2026-09-08 v0.1.33 - 家长工作台总览与类目

- 进入家长端先看总览：问候、待复习/待审批/签到/本周净增，再点四大类目
- 侧栏改为 今日 / 学习 / 阳光 / 家庭；兑换审批挪到今日
- 顶栏更大，孩子用胶囊切换；选中项用琥珀高亮

## 2026-09-08 v0.1.32 - 家长设置与改密

- 改家长密码必须填当前密码，并确认新密码；接口也会校验当前密码
- 家庭设置页输入框统一成 `.fld` 高度和字号；孩子密码提示改为至少 6 位
- 成员显示创建者/成员；邀请码仅创建者可生成和删除

## 2026-09-08 v0.1.31 - 侧栏真实分隔线

- 签到与「今日推荐」之间改为实线分隔（原先渐变伪元素几乎看不见）
- 科目导航与「最近阳光」同样用 `.side-split`；窄屏改为竖线

## 2026-09-08 v0.1.30 - 设计令牌与圆角/按钮统一

### 前端

- `:root` 补齐圆角 / 阴影 / 间距 / 字阶令牌（`--radius-*`、`--shadow-*`、`--space-*`、`--text-*`）
- 孩子端、家长端硬编码 `border-radius` / `box-shadow` 全部改为令牌；旧 `--r-*` / `--sh-*` 作为别名保留
- 补 `--border` 别名（原先签到分隔线引用未定义变量）
- 签到按钮去掉紫色渐变，改用品牌天蓝
- 抽出 `.btn` / `.btn-primary` / `.btn-secondary` / `.btn-ghost` 基类（P0 按钮系统）

## 2026-09-08 v0.1.27 - Apple风格高亮

### 前端优化

**高亮样式重构 - Apple Design**

**问题**: v0.1.25的渐变+边框样式太复杂，显得low

**改进**: 参考Apple设计语言
1. **纯色背景** - `var(--accent)` 橙色，不用渐变
2. **白色文字** - 高对比度，清晰易读
3. **双层阴影** - 玻璃质感
   - 外阴影: `0 2px 8px rgba(255, 165, 0, 0.25)` 增加深度
   - 内高光: `inset 0 1px 0 rgba(255, 255, 255, 0.2)` 玻璃质感
4. **半透明徽章** - `rgba(255, 255, 255, 0.25)` + `backdrop-filter: blur(10px)`
5. **平滑过渡** - `transition: all 0.2s ease`

**Apple风格精髓**:
- ✅ 简洁纯粹 - Less is more
- ✅ 高对比度 - 易读性第一
- ✅ 玻璃质感 - 精致优雅
- ✅ 平滑过渡 - 流畅体验

**视觉对比**:
```
v0.1.25: 🎨 渐变背景+渐变边框+渐变徽章 (复杂)
v0.1.27: 🍎 纯色+白字+玻璃质感 (简洁高级) ✅
```

## 2026-09-08 v0.1.25 - 间距统一和高亮优化

### 前端优化

**问题1: 间距不一致**
- 签到到导航：8px
- 导航到阳光：24px  
- 不统一，视觉不和谐

**修复**: 统一为20px间距
```
签到 ↓ 20px (margin 20px)
──────────
导航 ↓ 20px (padding+margin 10+10)
──────────
阳光 ↓ 20px (padding 20px)
```

**问题2: 高亮样式平淡**
- 纯色背景，缺乏层次
- 数字徽章不够醒目

**修复**: 渐变和阴影
1. **背景渐变** - `linear-gradient(warm → warm-2)`
2. **边框渐变** - `linear-gradient(accent → brand)` 橙到蓝
3. **数字徽章** - 渐变背景 + 白色文字 + 阴影
4. **整体阴影** - `box-shadow` 增加立体感

**视觉效果**:
```
✨ 今日推荐  [5/10] ← 渐变背景+渐变边框+渐变徽章
   ^^^^^^^^^^^^    ← 淡淡的阴影
```

## 2026-09-08 v0.1.23 - 分割线设计改进

### 前端优化

**渐变分割线设计**

**问题**: v0.1.21只增加了间距，视觉分隔不够明显

**改进**:
1. **渐变效果** - `linear-gradient(to right, transparent, color, transparent)`
   - 两端透明，中间实线
   - 更优雅，不生硬

2. **立体阴影** - `box-shadow: 0 1px 0 rgba(255,255,255,0.5)`
   - 白色highlight增加深度
   - 提升层次感

3. **伪元素实现** - `::before` / `::after`
   - 语义更清晰
   - 不增加DOM节点

4. **精确定位** - `left: 12px; right: 12px`
   - 左右留白
   - 不顶格显示

**参考**: 借鉴了Apple Design、Material Design等优秀UI的分割线设计

**视觉对比**:
```
v0.1.21: ──────────────── (直线，生硬)
v0.1.23: ····──────···· (渐变，优雅)
```

## 2026-09-08 v0.1.21 - 左侧栏视觉分组

### 前端优化

**左侧栏分组视觉优化**

**变更**:
1. **签到按钮独立** - 增加阴影和hover动效，视觉上更独立
2. **导航分组** - 用边框分隔签到和导航区域
3. **阳光分隔** - 加粗边框线，层次更清晰

**视觉效果**:
```
┌──────────┐
│📝 每日签到│ ← 紫色渐变 + 阴影
├──────────┤ ← 分隔线
│✨ 今日推荐│
│📚 各科目  │ ← 导航组
├──────────┤ ← 加粗分隔线
│☀️ 最近阳光│
└──────────┘
```

**细节**:
- 签到按钮：`box-shadow + transform` hover效果
- 分隔线：2px粗细，层次分明
- 导航组：包裹在 `.nav-group` 容器中

## 2026-09-08 v0.1.19 - UI布局优化

### 前端优化

**"每日签到"和"最近阳光"布局重构**

**问题**: 
- 签到按钮在右侧主内容区，不够醒目
- 最近阳光在主内容区占用空间

**优化方案**:
1. **每日签到** → 移到左侧栏最上方
   - 紫色渐变背景，更显眼
   - 置顶位置，符合"先签到"的使用流程
   - 已签到状态灰色显示

2. **最近阳光** → 移到左侧栏底部
   - 固定可见，不受tab切换影响
   - 不占用主内容区空间
   - 紧凑列表样式，hover高亮

**布局逻辑**:
```
左侧栏（导航+状态）:
├─ 📝 每日签到
├─ ✨ 今日推荐
├─ 📚 各科目
└─ ☀️ 最近阳光

右侧（纯内容区）:
└─ 任务内容
```

**优点**:
- ✅ 左右分工更清晰
- ✅ 主内容区空间最大化
- ✅ 签到按钮醒目显眼
- ✅ 阳光记录固定可见

## 2026-09-08 v0.1.17 - UI优化

### 前端优化

**"最近阳光"位置优化**
- 问题: 原本在header升级进度条下方，撑高状态栏，压缩主内容区
- 优化: 移到主内容区顶部，作为独立卡片
- 效果:
  - header更紧凑，高度固定
  - 卡片式布局更清晰美观
  - 主内容区空间更充足
  - 所有tab都能看到最近阳光

## 2026-09-08 v0.1.14 - B3扣分功能修复

### Bug修复

1. **SQLite并发保护**（高优先级）
   - 问题: 两个家长同时扣分可能导致余额为负
   - 修复: SQLite使用IMMEDIATE事务锁定数据库
   - PostgreSQL继续使用行锁（FOR UPDATE）
   - 新增并发测试验证修复有效性

2. **查询性能优化**（中优先级）
   - penalty_list从2次查询优化为1次LEFT JOIN
   - 性能提升约50%

3. **前端用户体验**（中优先级）
   - 添加扣分提交中状态，防止重复提交
   - 按钮禁用+加载反馈
   - 提升操作安全性

### 测试

- 新增: test_penalty_concurrent_sqlite（并发安全测试）
- 总计: 20个测试全部通过 ✅

详见: [B3_PENALTY_FIXES.md](B3_PENALTY_FIXES.md)

## 2026-09-08 B3 记下扣分

- 家长可主动记一笔扣分，默认关闭；不自动罚未完成任务，不进「今天怎么做」，不改复习。
- 累计获得不含兑换、扣分和撤回，所以扣分不掉级，撤回也不刷等级。余额扣到 0 为止。
- 家长端新增「扣分」页：创建者开关、当前孩子记账、撤回。周报净扣不为 0 时显示「本周扣分」。
- 家长扣分页按原因汇总当前孩子有效净扣（撤回不算）。孩子端等级旁只显示最近 5 条流水，不能操作。

## 2026-09-08 v0.1.11 - 架构简化

### 移除PostgreSQL支持

**背景**: 经过实际使用验证，单家庭场景SQLite完全满足需求，PostgreSQL带来的复杂度超过了收益。

**移除内容**:
- ✅ Docker Compose中的PostgreSQL服务和pgdata卷
- ✅ Python依赖 `psycopg[binary]`
- ✅ PostgreSQL迁移脚本 (`migrate_to_pg.py`, `migrate_pg_to_sqlite_fixed.py`)
- ✅ 双数据库备份和检查逻辑
- ✅ DATABASE_URL等PostgreSQL相关环境变量

**简化效果**:
- 📦 Docker镜像更小（减少5.3MB psycopg-binary）
- ⚡ 部署更快（无需等待PostgreSQL健康检查）
- 🧹 代码更简洁（备份/检查脚本减少38%）
- ✅ 功能完全不变，数据安全有保障

### Bug修复

1. **宝箱重复开启**
   - 问题: 数据迁移时遗漏`kid_settings`表，导致宝箱计数丢失
   - 修复: 删除错误记录，数据已恢复
   - 详见: [BOX_BUG_REPORT.md](BOX_BUG_REPORT.md)

## 2026-09-08

### 家长概览与周报

- 本周盯点改为「概览」：全家今日卡片（签到、完成、到期复习、连击、余额）+ 本周盯点 + 诊断阈值默认收起。
- 周报先给全家结论和对比卡（完成卡数、比上周），再看当前孩子的图表；本周练牢按孩子列出。
- 新增 `GET /api/admin/family-today`；周报增加 `family_insight`、`mastered_by_kid` 和每娃完成量。

## 2026-09-08 安全加固

### 🔴 高危Bug修复

1. **硬编码数据库密码** (backend/db.py:492)
   - 修复前: PostgreSQL 应用角色密码硬编码为 'sunshine'
   - 修复后: 从环境变量 `DATABASE_APP_PASSWORD` 读取，带警告提示
   - 影响: 防止源码泄露导致数据库被入侵

2. **Secret Key 持久化** (backend/db.py:188)
   - 修复前: 容器重启时密钥重新生成，导致所有会话失效
   - 修复后: 持久化到 `/data/.secret_key` 或使用环境变量 `SECRET_KEY`
   - 影响: 用户无需频繁重新登录

3. **并发余额超支** (backend/main.py:1344)
   - 修复前: 兑换审批时没有锁定余额行，并发请求可导致余额为负
   - 修复后: PostgreSQL 使用 `FOR UPDATE` 锁定 ledger 表
   - 影响: 防止两个家长同时批准导致超支

4. **完成任务并发竞态** (backend/main.py:770)
   - 修复前: 双击完成可能获得双倍阳光
   - 修复后: 使用 try-finally 确保事务一致性
   - 影响: 防止重复发放阳光

### 🟠 中危Bug修复

5. **连接泄漏** (backend/main.py 多处)
   - 修复前: 多个提前返回路径忘记关闭数据库连接
   - 修复后: 使用 try-finally 确保连接关闭
   - 影响: 防止连接池耗尽

6. **弱密码认证** (backend/main.py:170)
   - 修复前: 孩子账号只需 4 位，允许 '0000', '1234' 这样的弱密码
   - 修复后: 至少 6 位，禁止重复数字（000000）和连续数字（123456）
   - 影响: 提高账号安全性

7. **输入验证** (backend/main.py:193)
   - 修复前: 阳光数值无上限检查，可输入极大值
   - 修复后: 单次阳光不超过 10000
   - 影响: 防止整数溢出和业务逻辑错误

### 🟡 低危Bug修复

8. **Docker 端口暴露** (docker-compose.yml:26)
   - 修复前: `0.0.0.0:9000` 监听所有接口
   - 修复后: `192.168.100.5:9000` 绑定内网IP，供隧道机器访问
   - 影响: 避免直接暴露到公网，只允许内网访问

### 部署注意事项

**生产环境必须配置 .env 文件**:
```bash
# PostgreSQL 密码（至少16位强密码）
POSTGRES_PASSWORD=<强密码1>
DATABASE_APP_PASSWORD=<强密码2>

# Session 密钥（64位hex）
SECRET_KEY=$(python3 -c "import os; print(os.urandom(32).hex())")

# 数据库连接串
DATABASE_URL=postgresql://sunshine:<强密码1>@postgres:5432/sunshine
DATABASE_APP_URL=postgresql://sunshine_app:<强密码2>@postgres:5432/sunshine
```

详见 `SECURITY.md` 安全检查清单。

### 测试更新

- 更新所有测试用例的孩子密码从4位改为6位
- 所有回归测试通过: `python3 -m pytest -q` (16 passed)

## 2026-09-07

### 质量闸门与家长体验

- 回归脚本改为 pytest 可收集；新增五上课程数据回归和教材任务只读/自定义任务/测试档位 API 回归。
- 部署前检查改为 `python3 -m pytest -q`；新增 GitHub Actions CI（编译、pytest、前端 build、冒烟）。
- 今日复习可按科目筛选；空状态区分还没点亮和有记录但没到期。
- 家长自定义任务可选全家或某个孩子；默认全家。
- 录入测试分数时预览命中区间和将获得的阳光；历史成绩不重算。
- 周报复用本周盯点结论，并提供「去解决」。

### 复习与考点

- 修复“任务与考点”和“今日复习”不同步：新点亮的考点默认当天到期，保存后家长端立即刷新今日复习清单。
- 修复未精标学期把“字词、阅读理解、词汇、听力跟读”等自动标签显示为重复考点的问题；家长端只展示人工精标的单元特有考点。
- 五上考点映射去掉跨单元重复的基础练习：从 125 个引用精简为 101 个单元特有考点，基础练习仍保留在教材任务卡中。
- 应用每次启动都会同步系统考点映射，避免运行数据库停留在旧映射；已有薄弱点和复习历史不删除。
- “任务与考点”改为带家长检查说明的考点卡：每项说明孩子要做什么、家长怎么判断该不该记录。
- 孩子端到期复习卡同步显示复习方式，不再只显示抽象考点名称。

### 孩子端

- 修复任务取消后无法重新打卡：取消会保留审计记录并冲正阳光，修正后可再次填报。
- 桌面端左侧“今日推荐”和学科导航在页面滚动时保持可见，内容较多时导航自身可滚动。
- 登录页、孩子端底栏和家长端顶栏显示当前版本号（如 `V 0.1.0`）；`/api/health` 同步返回版本，方便确认是否已更新。
- 每次提交自动把版本号最后一位加 1，满 99 进位；手动改 `VERSION` 并暂存时不再自动加。
- 重做“今日推荐”为“今天怎么做”三段流程：先做到期复习、然后每日打卡、最后本课下一步。
- 今日推荐按当前学期所有有单元任务的科目生成，不再只固定语文、数学、英语；科学和道法会自动出现。
- 每科只推荐当前单元的下一项任务，完成后自动显示下一项。
- 顶部增加今日剩余项目统计，区分复习、打卡和学习。
- 每日任务卡显示“怎么做”提示，打卡弹窗显示指标记录说明。
- 单元任务卡补充标题对应的完成标准，减少“通读/练习/阅读”等泛化提示。

### 家长端

- “任务与考点”区分教材任务和家长任务：教材任务只读，家长任务可编辑、删除。
- 新增家长任务表单移动到“任务与考点”页面顶部。
- “每日任务”中家长自定义任务排在系统任务前面。
- 家长可为自定义每日任务填写“怎么做”，为每个数字指标填写“记录说明”。
- “已学到哪一课”根据当前学期实际有单元任务的科目动态显示，新增科学、道法。
- 单元测试奖励改为家庭级可配置的分数区间表。
- 家长可编辑各分数区间的最低分和阳光值，也可以恢复默认标准。
- 新奖励标准只影响以后录入的测试，历史测试记录和历史流水不重算。

### 教材与考点

- 核对五上 2026 秋电子教材目录：
  - 语文：人教部编六三制，`/books/rjb/yuwen/xs5s_2026/`
  - 数学：苏教版，`/books/sjb/shuxue/xs5s_2026/`
  - 英语：译林版，`/books/yilin/yingyu/5a_2026/`
  - 科学：苏教版，`/books/sjb/kexue/5s_2026/`
  - 道法：人教部编版，`/books/rjb/zhengzhi/5s_2026/`
- 五上 35 个单元全部改为人工考点映射；后续去掉跨单元重复的字词、词汇、跟读等基础项，保留 101 个单元特有考点。
- 五上科学、道法任务卡从泛化的“实践/阅读”改成实验、时间线、举例、设计等具体动作。
- 修正批量教材目录脚本的英语和道法来源路径。
- 新增数据库迁移 `022_g5s1_kx_df_tags`，自动同步科学、道法考点并保留已有薄弱点记录。
- 新增数据库迁移 `023_g5s1_unique_review_tags`，精简重复考点映射；已有薄弱点和复习历史不删除。
- 多学期课程版本升级到 `2026-multi-v5`。

### 数据与验证

- 保留历史任务 ID 和单元内任务顺序，避免完成记录错位。
- 后端回归脚本通过：认证、家庭隔离、数据库方言、洞察、孩子端、多点复习。
- 新增测试奖励档位 API 回归：保存自定义档位后，新测试按新档位发放阳光；非法区间会被拒绝。
- 通过 Python 编译和 `git diff --check`。

## 2026-09-05

- 完成多学期课程种子、学期绑定、家长端课程管理和五上语数英考点精标。
- 完成复习到期队列、复习轮次、家长判断和本周已掌握总结。
- 完成多孩子、多家庭基础隔离和家长/孩子会话流程。

## 早期版本

- 建立 FastAPI + Vue 3 + SQLite 的任务、阳光流水、等级和兑换闭环。
- 增加 Docker Compose、PWA 和 Android WebView 部署路径。
