# 后续计划

> 当前基线：五上语文、数学、英语、科学、道法已经按 2026 秋电子教材目录生成任务和人工考点；孩子端、家长端和复习闭环已经可用。下面只列尚未完成或仍需复核的工作。

## 已完成

- 多孩子、家庭成员和家庭级配置基础能力
- 任务完成、取消冲正、阳光流水、等级、兑换审批和周报
- 五上五科：35 个单元、149 张任务卡、101 个单元特有考点
- 薄弱考点记录、到期复习、家长判断和复习轮次
- 教材任务只读、家长自定义任务管理，可选全家或指定孩子
- pytest 回归、课程数据校验和 GitHub CI
- 今日复习按科目筛选、测试录分预览阳光、周报复用本周盯点结论
- 家长概览全家今日卡片、周报全家对比和家庭结论
- 每日任务提示、指标记录说明和个人纪录
- 单元测试分数区间奖励的家庭级配置
- 孩子端“今天怎么做”流程和家长端动态课程游标

## P0：上线前验证

目标：确认当前源码在真实 Node、浏览器和 Android WebView 上可用。

1. 在完整 Node.js 环境执行 `npm --prefix frontend run build`。
2. 启动前后端，验证孩子端“今天怎么做”三段流程：到期复习、每日打卡、本课下一步。
3. 用手机宽度和 Android WebView 检查：孩子端任务卡、每日任务弹窗、家长端考点、测试奖励区间表、每日自定义任务表单。
4. 对现有运行数据库确认：`2026-multi-v5` 课程刷新和迁移 `022_g5s1_kx_df_tags`、`023_g5s1_unique_review_tags` 已经生效。
5. 在生产库备份副本上验证课程刷新不删除自定义任务、完成记录、阳光流水和弱项记录。

验收：构建成功；关键页面无 Vue 错误、溢出或遮挡；五上五科任务和考点可从实际 API 读取。

## P1：测试基础设施（已完成）

- 现有 `backend/test_*.py` 已改为 pytest 可收集的 `test_*`。
- 覆盖认证、跨家庭隔离、任务取消冲正、多孩子、复习队列、测试奖励档位。
- 课程回归：五上 35 个单元、149 张任务、101 个单元特有考点、无空映射、无跨单元重复、任务 ID 稳定。
- API 回归：教材任务不可修改；自定义任务可管理并可指定孩子；自定义测试奖励仅影响新测试。

验收命令：在 `backend/` 下执行 `python3 -m pytest -q`。

## P2：教材内容持续维护

目标：以教材实物和官方资源继续校验内容，不凭空扩展。

1. 用孩子手上的实物教材复核五上语文单元要素、英语 Grammar/Sounds、科学实验器材要求和道法表述。
2. 确认五年级下册 2027 春新版目录上线后，抓目录、生成任务、人工补考点并升级课程版本。
3. 确认南通实际使用的音乐、美术版本后，再决定是否加入单元任务和考点。
4. 综合实践继续保持家长自定义任务，除非学校提供稳定教材或课程纲要。
5. 为新学期增加任务 ID 连续性、旧完成记录隔离和迁移回归检查。

验收：每次新增教材均有可追溯目录入口、生成脚本、人工考点映射和数据校验。

## P3：家长使用体验（已完成）

- 今日复习可按科目筛选；空状态区分「还没点亮」和「有记录但没到期」。孩子切换继续用顶栏。
- 自定义任务可选「全家」或某个孩子；默认全家。
- 录入测试分数时预览命中的分数区间和将获得的阳光；历史成绩不重算。
- 周报复用本周盯点那一句可解释结论，并可跳到复习或测试。

验收：家长能在一个页面完成“看问题、安排练习、判断结果”，不需要理解内部术语。

## P4：部署与运维

目标：让更新和恢复可复现。

1. ~~在 CI 或部署前执行 Python 编译、回归测试、前端 build 和前端冒烟检查。~~ 已完成：`.github/workflows/ci.yml` 与 `scripts/pre_deploy.sh`。
2. 把生产数据库备份、恢复演练和迁移校验写成可执行脚本。
3. 明确 SQLite 与 PostgreSQL 的生产选择，并为实际选择保留一套已验证部署流程。
4. 补充版本发布步骤：更新 `CHANGELOG.md`、运行验证、打 tag、发布 APK/WebView 壳。

验收：任何一次课程或前端更新都能在部署前发现构建失败，并可从备份恢复。

## 支线 B：激励 / 协同

目标：让家长一眼看全家、必要时一起完成一件事。不另做一套游戏，不改复习和考点规则。当前范围以本节为准；`MULTIUSER_PLAN.md` §7.5 只作对照。

铁律：ledger 仍是唯一真相；消费不掉级；余额不能为负；诊断权在家长；孩子端仍是「今天怎么做」；默认全家、可选指定孩子；先自用后传播。

### 落地顺序

1. **B1 全家今日面板**（已完成，与 B2 同一次提交）

   不新增侧栏。把「本周盯点」改名为「概览」，同一页三段：全家今日 → 本周盯点 → 诊断阈值（默认收起）。点今日卡 = 现有 `pickKid`。

   每娃一张卡，按创建时间排，不按余额/完成数排。字段：名字；状态条（有到期复习→琥珀「今日复习 N 项」，否则未签到→灰「还没来」，否则→绿「今天来了」）；今日完成数（当日 completed，取消不算）；到期复习数；连击；余额。复习 > 0 才显示「去复习」。

   空状态：没孩子沿用「还没有孩子。」；都已签到且无到期复习时，1 个孩子写「今天来了，没有到期复习。」，多个孩子写「今天都来了，没有到期复习。」

   接口 `GET /api/admin/family-today`：`{ today, kids: [{ kid_id, name, checkin, completed_today, review_due, streak, balance }] }`，服务端按家庭 roster 聚合。不在这页发阳光/扣分，孩子端不展示其他娃。

2. **B2 周报全家对比**（已完成，与 B1 同一次提交）

   周报仍单独一页，先全家、再当前娃。禁止名次文案（第一/冠军/落后/PK）；环比写成「比上周多 N 张 / 少 N 张 / 和上周差不多」。

   上半：家庭结论一句（对每娃跑 `build_insights()`，按到期复习 > 低分单元 > 体测 > 连击 > 完成量下滑排序，最多两娃，如「先看乐乐的复习（3 项到期），再看弟弟的语文测验。」；都无命中则「这周不用特别盯。」）；对比卡补完成卡数和比上周；本周练牢改为「乐乐：竖式进位；弟弟：like doing」。下半保留单娃图表，标题加当前孩子名。

   扩展 `GET /api/admin/weekly`：`kids[]` 增加 `completed`、`completed_last`、`insight`；顶层增加 `family_insight: { text, action, kid_id }`、`mastered_by_kid`。本周窗口=本周一到今天，上周=上周一到上周同一进度日。不做月报、CSV、自动推送。
3. **B3 扣分**（第二刀，默认可关）：家长端记一笔，`reason='penalty'` 不计入累计获得故不掉级；扣到 0 为止；可冲正为 `penalty_cancel`。不做自动扣、孩子互扣、扣到负。
4. **B4 家庭共同目标**（第三刀）：同时只 1 个进行中目标；指标只用完成卡数、运动次数、签到天数；达标每人发 0–20 阳光（默认 5，`reason='family_goal'`，只发一次）；过期关闭不扣分。孩子端只在「今天怎么做」顶部显示「全家还差 N 次」。

B1+B2 不改阳光规则。B3 必须有开关、冲正和「不掉级」测试。B4 要防重复发放。

### 本支线不做

- 多娃排行榜、周冠军加阳光
- 1–3 星质量评分
- 专注计时 / 番茄钟
- 长辈红包
- 月报 CSV、成长相册、战报分享、班级模式（属重线 C）

## 不做

- 不新增第二套学习推荐算法：孩子端只展示已有复习、每日任务和当前单元下一步。
- 不为没有确认教材版本的音美编造任务或考点。
- 不让孩子修改教材任务、阳光标准或测试奖励标准。
- 不重算历史测试奖励、历史阳光流水或已经完成的复习记录。
- 不做排行榜、星级评分、专注计时、长辈红包（见支线 B）。
