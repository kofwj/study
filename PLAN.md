# 阳光学习工作台 — 设计思路（未写代码）

> 目标：让娃自主打卡、完成任务赚「阳光」、兑换奖励、升级打怪。**跨学期、跨年级长期可用。**

---

## 1. 开源调研结论

| 项目 | 技术栈 | 亮点 | 哪里不匹配 |
|---|---|---|---|
| **gischuck/learning-system**（鸡娃系统，中文） | React + Express + SQLite | 习惯打卡、星星等级、心愿商城、课程表、家长端审核、AI 问答 | 是「习惯/作业」模型，没有「学科→单元→任务」的教材结构；星星逻辑硬编码 |
| **ChoreQuest**（英文） | FastAPI + React | RPG 化最全：XP、8 段等级(Apprentice→Mythic)、连击/漏打冻结、金银铜成就、宠物、转盘、审核商店 | 全部是「家务任务」，英文界面，无学科/教材概念 |
| **Pointsy** | Next.js + TS | 移动端 PWA、多家庭、家务赚积分兑换 | 家务导向，游戏化弱 |

**结论：没有一个是「改改就能用」的。**
- 现成项目的核心数据模型都是「习惯/家务任务」，和你需要的「8 学科 × 教材单元 × 任务」根本不是一类东西。
- 它们的可复用价值在 **UI 风格和游戏化玩法**（等级段、连击冻结、兑换审核、转盘、成就），不在数据层。

> 参考代码已 clone 到 `_ref/`（learning-system / chorequest / pointsy），只作借鉴素材，确定方案后可删。

---

## 2. 决策：自建薄的、对的系统（不 fork 大改）

fork 别人 2 万行家务 App、硬塞教材结构 = 改一半留一半，最难维护。

自建的核心只有一套**数据模型 + 一个流水账**，其余都是现成组件。你的需求（8 科按单元拆任务、长期用、公平扣回）恰恰**只能靠把这套数据模型做对**来实现，而这是现成项目都不具备的。

**复用两条路（省钱）：**
- 技术栈用你自己已经会的：**FastAPI + Vue3 + SQLite**（和 invest-tracker 一致），部署复用 Cloudflare Tunnel / Docker VPS 那套现成 skill。
- 游戏化玩法「借鉴」ChoreQuest 的设计（等级段自定、连击冻结、兑换审核），但只抄想法，不抄代码。

---

## 3. 「一直用下去」的关键：内容全部是数据，不是代码

学期、学科、单元、任务、商店、等级，全部存数据库。明年换「五年级下」「六年级上」，**只加数据行，不动代码**。

### 数据模型（草稿）

```
学科 subject        （语文/数学/英语/科学/道法/体育/音美/综合）—— 8 行，可增可改
学期 term           （五年级上 · 江苏南通）—— 加「五年级下」就是再加一行
   └─ 单元 unit     （第一单元…） 属于某学科某学期
        └─ 任务 task （背诵《白鹭》/ 口算 100 道…） 类型 + 默认阳光 + frequency(once/daily)
             ├─ once：单元任务，打完一次即完（三科主体）
             └─ daily：每日循环（如体育跳绳），可选 metrics 记录数字（100下用时/1分钟个数等）

孩子 kid            （单娃为先，多娃后期）
今日签到 checkin    （日期 + 领到的阳光）
任务完成 task_completion （孩子×任务×日期 + 状态：completed / cancelled）
阳光流水 ledger     （**真相源**，append-only，不可删改）
   └─ 每条: 孩子×日期×变动(+5/-5)×原因×关联单据×变动后余额快照
商店商品 reward     （看动画30分钟=30 / 游乐园=100 / 心愿礼物=200…）分类 + 是否需审核
兑换记录 redemption （孩子×商品×日期×花费×状态：申请/已批准/完成/拒绝）
等级配置 rank       （阳光萌新 → 传奇学神，每级一个累计阳光阈值）
```

### 三条「对」的设计（保证公平 + 长期不出 bug）

1. **流水账是唯一真相源**，等级、余额、连击天数全部「推导」出来，不单独硬存。
   - 点错取消 = 补一条 `-5` 流水 + 把 completion 置为 cancelled，**不删历史**。娃自己能看到「我 +5 又 -5，归零」，才认账。
2. **等级看「累计获得」，不看「当前余额」**。用阳光换了大奖不能掉级，否则娃换完游乐园从王者跌回青铜会直接弃坑。
3. **连续打卡天数靠日期推**（一串连续日期），不存 streak 数字，漏一天也不会出现幻影连击。

---

## 4. 你要的 6 个功能 → 具体模块

| 你的想法 | 落地 |
|---|---|
| 🌞 每日打卡领阳光 + 连续天数 | `checkin` 表 + 由日期推导连续天数 |
| 📚 8 学科按单元拆任务 | 五层结构 `学科→学期→单元→任务`，数据驱动 |
| ✅ 任务 +5 / 点错 -5 | `task_completion` + `ledger` 双向流水，公平可审计 |
| ⭐ 今日推荐自动挑没做完的 | 查询：当前学期 active 任务里，今天/本周未完成的，按学科排 |
| 🛒 阳光兑换商店，明码标价 | `reward` 表 + `redemption` 表；大额(≥100)走家长审核 |
| 📈 等级成长 萌新→学神 | `rank` 阈值表 + 由累计阳光推导，配对应等级名/图标 |

---

## 5. 技术栈 & 部署（复用你现成的）

- 后端 FastAPI + SQLite（单文件库足够，家庭用量）
- 前端 Vue3 + Vite + **PWA**（装到手机/iPad 桌面，娃点开即用）
- 部署：Docker + Cloudflare Tunnel 那套现成流程

---

## 6. 分阶段路线

- **P0 能跑通闭环**：打卡领阳光 → 看到 8 科任务 → 点完成 +5 / 取消 -5 → 商店兑换 → 等级随累计阳光升。单娃、单学期、家长手动录入五上内容。
- **P1 体验**：今日推荐、连续天数展示、兑换审核流、等级图标动画、移动端 PWA。
- **P2 长期化**：学期切换（一键新建下学期）、内容录入模板（把 8 科任务做成可导入的 CSV/JSON）、简单统计图表（周报）。
- **不做（YAGNI）**：AI 问答、多孩子、推送通知、宠物/转盘——娃真玩腻了再加。

---

## 7. 已拍板（定稿）

1. **实现路线**：自建薄系统（不 fork），FastAPI + Vue3 + SQLite
2. **部署**：现有 VPS，Docker + Cloudflare Tunnel
3. **等级规则**：按累计获得阳光，消费不掉级
4. **设备**：移动端 PWA + 桌面自适应，两端都好用