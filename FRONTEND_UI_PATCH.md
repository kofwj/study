# 前端 UI 权限

后端已用 `require_owner` 拦住敏感操作。页面侧：

- 删除孩子、删除成员、生成/删除邀请码、邀请保护、扣分开关：仅创建者看得到按钮；成员看到一句「只有创建者…」
- 成员列表：创建者可「交给创建者」或「删」
- `POST /api/admin/transfer-owner`，`api.admin.transferOwner(id)`

单家庭只有一个家长时，这些按钮都在，转让不会出现（没有其他成员）。
